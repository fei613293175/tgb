# 00 项目章程

## 1. 项目定义

这是一个基于 Discuz! X3.4、主要由 Discuz 插件提供业务页面的 H5 全站视觉重构与 Android 原生承载项目。产品统一名称为“推广宝”。

项目只改变用户可见表现：

- 配色、背景、层级
- 字号、字重、行高、字体回退
- 卡片、按钮、输入框、标签、导航、弹层等组件外观
- 布局间距、对齐、响应式和安全区
- 图标、图片装饰和状态视觉
- 加载、空态、错误、成功、禁用、选中和按压反馈

项目不改变：

- 业务能力、业务流程、字段含义和业务规则
- Discuz/插件路由及其参数
- 表单 action、method、字段名、隐藏字段和 `formhash`
- 登录、权限、支付、金额、积分、红包、分红、奖励、审核、提现算法
- 数据库结构、已有数据、插件启停状态和后台配置
- 接口请求/响应结构和第三方回调
- 外链目标

项目新增但不重写业务的原生能力：

- Kotlin Android WebView 容器；
- H5 相册图片上传；
- 支付宝等白名单第三方 App 拉起和返回；
- 状态栏/导航栏安全区；
- 推广宝图标、启动页、离线页和错误页；
- 不低于 10 MiB 的签名 Release APK。

## 2. 产品范围

范围只包含普通用户能够从当前生产 H5 的真实可见控件逐级点击、跳转、弹出或经合法状态自然进入的页面和组件状态。除根入口外，每个页面必须在 `17_RUNTIME_CLICK_GRAPH.csv` 中有完整父边；源码存在、直接 URL 可打开、隐藏 DOM 或插件启用均不构成范围证据。范围包括经点击图证明的：

- 未登录登录/注册/短信登录/协议页
- 首页、搜索、分类、列表、筛选、详情、成员主页
- 发布、上传、编辑、我的发布、审核、举报
- 个人中心、设置、资料、地址、认证、注销确认
- 消息列表、聊天详情、私信相关弹层
- 钱包、明细、提现、订单、会员和推广购买界面
- 邀请、团队、签到、分红、任务、名片等插件页面
- 底部导航、头部、通用弹层、Toast、空态、加载态、分页

默认不包含：

- Discuz 管理后台和插件管理后台
- 不可由用户前端进入的运维工具
- 没有真实可见父入口的插件页、直接 URL 页、隐藏 DOM、AJAX 分片和源码残留页
- 外部域名页面的内部 UI
- 业务功能新增、删除或重构
- Discuz/PHP/MySQL 大版本升级
- 现有安全暴露整改本身

Android App 本身属于范围；原生重写 H5 业务页面不属于范围。

上述范围外项目必须由站点负责人书面授权后进入新的任务编号。

## 3. 成功标准

同时满足以下条件才算完成：

1. `03_PAGE_LEDGER.csv` 中所有 `IN_SCOPE` 页面均达到 `REDESIGNED_VERIFIED` 或有负责人签字的豁免。
2. 新站不再使用当前暖橙、奶油底、重度胶囊圆角的主体视觉。
3. 所有业务路径与改造前功能等价，测试证据覆盖成功、失败、空态和权限态。
4. 360、390、430px 三个 H5 宽度无横向溢出、窄列竖排、遮挡和不可点击问题。
5. 重要触控区域不小于 44×44px，文本对比和键盘焦点满足门禁。
6. 运行时不依赖 Tailwind CDN、Font Awesome CDN 或新增公共 CDN。
7. 每个版本都具备改前/改后证据、回归结果、变更清单和回滚证明。
8. 生产发布采用可回滚方式，发布后完成真实 H5 冒烟检查。
9. 关版完成全局漂移审计；发现漂移时先加固规则再开下一版。
10. 新电脑或新 AI 仅凭本包和“继续开发”“立即开发”或“立即开始”任一触发词即可确定当前状态、下一任务和禁止事项。

## 4. 实施原则

- 先清点、后令牌、再组件、最后页面。
- 先从原平台真实点击建立页面图，再做源码映射；源码映射不能反向扩大视觉范围。
- 先公共低风险页面，再核心内容，再账户，再资金和高风险链路。
- 改模板表现，不碰处理程序业务分支。
- 公共视觉变量集中定义，不在 164 个模板中复制散乱魔法值。
- 原生 CSS/渐进增强优先，兼容旧 Android WebView 和微信内置浏览器。
- 每次只推进一个版本；任何未闭环问题写入状态，不靠记忆。
- 线上只接收已经在预发布副本验证并具备回滚材料的版本。

## 5. 角色责任

- 站点负责人：授权高风险测试、提供外部凭据、确认设计验收和生产发布时间。
- 开发执行者：遵守门禁、建立页面映射、实现视觉、保留功能等价证据。
- 测试执行者：使用隔离测试账号/测试数据，验证页面、业务和回滚。
- 接续 AI：先验证交接包，再执行唯一下一任务；不得从聊天猜状态。

## 6. 完成定义

单页面完成不是“看起来变了”，而是：

- 页面台账、模板映射、状态清单齐全；
- 设计令牌和组件规范正确复用；
- 页面全部可交互状态已覆盖；
- 功能前后请求与结果等价；
- 视口、溢出、无障碍、资源和控制台门禁通过；
- 证据路径写入关版记录。
