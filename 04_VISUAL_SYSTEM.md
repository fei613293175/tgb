# 04 新视觉体系：晴空电光 / Light Grid

## 1. 视觉目标

新风格应与现有“暖橙 + 奶油底 + 大胶囊”形成清晰断代，并严格保持浅色页面：

- 明亮、理性、清晰、轻科技；
- 浅灰蓝构建页面基底，电光蓝承担主动作；
- 薄荷青和紫罗兰用于层级与辅助强调；
- 页面、头部、卡片、弹层和启动页禁止大面积深色；
- 卡片更克制，圆角更小，阴影更轻；
- 高信息密度页面仍保持可扫描性；
- 资金、奖励、审核等状态使用稳定语义色，不以装饰色代替含义。

风格名称只用于设计沟通，不改站点品牌文案。

## 2. 颜色令牌

| 令牌 | 色值 | 用途 |
|---|---|---|
| `--tg-bg` | `#F4F7FB` | 页面背景 |
| `--tg-surface` | `#FFFFFF` | 卡片/输入/弹层 |
| `--tg-surface-soft` | `#EDF3FA` | 次级区域 |
| `--tg-ink` | `#0E1B2A` | 主文本 |
| `--tg-text` | `#405166` | 正文 |
| `--tg-muted` | `#718096` | 辅助文字 |
| `--tg-border` | `#D8E1EC` | 边框/分割 |
| `--tg-navy` | `#0C1B33` | 深色文字/小面积图标，禁止大面积背景 |
| `--tg-primary` | `#2764FF` | 主按钮/主链接 |
| `--tg-primary-pressed` | `#1847CC` | 按压态 |
| `--tg-mint` | `#19B8A9` | 辅助强调 |
| `--tg-violet` | `#7657FF` | 高级/会员强调 |
| `--tg-success` | `#15965A` | 成功/到账/通过 |
| `--tg-warning` | `#D99000` | 提醒/待处理 |
| `--tg-danger` | `#D93F5C` | 错误/拒绝/删除 |
| `--tg-info` | `#2176C7` | 信息状态 |

限制：

- 主界面禁止继续以暖橙和奶油底作为品牌色。
- 页面主体必须使用 `#F4F7FB`、白色或相邻浅色；不得提供深色主题。
- `#0C1B33` 仅限文字、描边、图标等小面积元素，不得用于整页、整块头部或启动页背景。
- 橙色只允许在具有明确“警告”含义且视觉审查通过时使用。
- 文本和交互对比度至少达到 WCAG AA：正文 4.5:1，大文本 3:1。
- 金额增加/减少不能只依赖红绿颜色，必须同时有 `+/-`、图标或文字。

## 3. 字体与字号

字体栈：

```css
font-family: "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
  system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
```

字号尺度：

| 名称 | 字号/行高 | 用途 |
|---|---|---|
| `xs` | 12/18 | 辅助、标签 |
| `sm` | 13/20 | 次要信息 |
| `body` | 14/22 | 默认正文 |
| `body-lg` | 16/24 | 输入、强调正文 |
| `title-sm` | 18/26 | 卡片组标题 |
| `title` | 22/30 | 页面标题 |
| `display` | 28/36 | 钱包余额、关键数字 |

规则：

- H5 输入控件字体不得小于 16px，避免 iOS 自动缩放。
- 正文不使用 400 以下字重；中文大段不使用过粗字重。
- 长标题最多按页面业务原规则截断，不因视觉重构改变完整值或数据。

## 4. 间距、圆角和阴影

- 基础网格：4px。
- 间距：4、8、12、16、20、24、32px。
- 页面左右安全边距：16px；紧凑表格可 12px，但不得小于 12px。
- 卡片圆角：16px；输入/按钮：12px；标签：6px；头像保持圆形。
- 禁止把所有按钮和卡片做成胶囊。
- 阴影：`0 8px 24px rgba(12, 27, 51, .08)`，仅用于浮层或关键卡片。
- 普通层级优先使用边框和背景差异，不堆叠阴影。

## 5. 核心组件

### 页面外壳

- 最大业务宽度仍保持与当前逻辑兼容，不重定义桌面业务版。
- 视口：360×800、390×844、430×932。
- 使用 `env(safe-area-inset-*)` 处理刘海和底部手势区。
- `box-sizing: border-box` 覆盖所有元素和伪元素。
- 图片、视频、iframe、表格最大宽度不得超过容器。

### 顶部栏

- 高 52px，加顶部安全区。
- 白底或极浅灰蓝两种变体；返回、标题、右操作固定布局。
- 不更换返回行为和 JavaScript 链接。

### 底部导航

- 内容高 64px，加底部安全区。
- 图标 22—24px，标签 12px。
- 当前项用电光蓝 + 细标记，不依赖大面积橙色底。
- 原有四项顺序、URL、权限完全保持。

### 按钮

- 主按钮高 48px、圆角 12px、最小触区 44×44px。
- 次按钮白底蓝框；危险按钮使用危险语义色。
- 必须定义 normal、pressed、focus、disabled、loading。
- 不改变原按钮 `type`、name、value、事件或提交目标。

### 输入和表单

- 输入高至少 48px，字体 16px。
- 标签、帮助、错误信息纵向排列，禁止依赖占位符替代标签。
- 文件上传、富文本、级联地址、短信验证码保持原控件协议。
- 隐藏字段和 `formhash` 不得因整理 DOM 被删除。

### 卡片与列表

- 卡片内边距 16px，分组间距 12px。
- 信息顺序服从业务原义；只能调整视觉层级。
- 长标题、金额、时间、状态必须在 360px 下可读。

### 弹层与反馈

- Dialog 圆角 16px，底部 Sheet 顶部圆角 20px。
- Toast、loading、success、error、confirm 均需统一。
- 旧插件注入的隐藏弹层不能直接删除，先建立触发映射。

## 6. 图标和资源

- 优先使用本站版本化目录托管的 SVG sprite 或内联 SVG；只使用经过许可的图标集。
- 必须保留 iconfont 时，只打包页面实际使用字符的 WOFF2 子集，采用内容哈希文件名、正确 MIME、`font-display: swap` 和长期缓存；不加载完整冗余字体。
- 新设计禁止在线 Tailwind CSS，不新增运行时公共 CDN；旧页在线 Tailwind 在所属版本迁移时删除。
- 首屏关键图标不得依赖第三方网络，需检查请求数、传输体积、缓存命中和 404。
- 新图片必须有来源/许可记录、尺寸和压缩门禁。
- 装饰插画不得覆盖按钮、输入、金额、二维码和法律文字。
- 不用纯图片承载必须可访问/可复制的文字。

## 7. 动效

- 仅使用 120—240ms 的轻量过渡。
- 页面主流程不依赖动效才能完成。
- 遵守 `prefers-reduced-motion`。
- 禁止引入影响旧 WebView 的重型动画库。

## 8. 页面族应用

- 公共登录：浅灰蓝品牌区 + 白色结构卡，不再使用奶油橙或大面积深色；优先修复窄列和脚本依赖。
- 首页/列表：浅灰蓝背景、清晰分类、紧凑内容卡、电光蓝主动作。
- 详情：内容阅读优先，联系方式/举报/红包明确分区。
- 账户：浅色身份概览 + 模块化功能网格，双钱包必须明确区分。
- 资金：数字和状态优先，交易类型使用语义标签，不做装饰性渐变干扰。
- 增长/会员：紫罗兰只用于会员或高级权益，奖励成功仍用成功色。
- 法律长文：纯白阅读面、16px 正文、1.75 行高、稳定段落宽度。

## 9. 技术实现约束

- 建议建立本地统一 CSS 令牌和组件层，再逐模板迁移。
- 不引入运行时 Tailwind CDN；如开发期使用离线工具生成 CSS，交付物必须是经过裁剪、版本化的本站静态 CSS，且不改变现有业务 DOM 协议。
- 不要求引入 React/Vue 等构建链；未经负责人批准不得更换框架。
- 必须兼容现有 PHP 模板、Discuz 模板语法、jQuery/WeUI 业务脚本。
- 对遗留选择器采用可控包装类，避免全局重置破坏插件弹层。
- 每个页面先做 DOM/事件基线，再改 CSS；需要改 HTML 时必须做字段与事件 diff。
