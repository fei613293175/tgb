# 09 远程访问与生产安全

## 1. 连接

已建立过的本机 SSH 别名：`obx-test`。

每台新电脑必须重新验证：

```powershell
ssh obx-test
```

进入后只读确认：

```bash
hostname
pwd
readlink -f /www/wwwroot/tg.suewammes.com
test -d /www/wwwroot/tg.suewammes.com/source/plugin
```

不得仅因为别名同名就假设目标服务器正确。主机指纹变化必须由负责人核对。

## 2. 敏感信息

- 因当前数据库标识与密码字符串重合，本包两者都不记录；接管时从受保护的生产配置只读获取。
- 站点登录、数据库、SSH、签名和第三方支付凭据均在外部安全渠道管理。
- 不把 `config_global.php`、`config_ucenter.php`、`.env`、keystore 或完整数据库导入交接包。
- 命令输出含 Cookie、formhash、UID、金额、手机号或身份资料时必须脱敏。

## 3. 预发布复制

R00 必须先确定：

- 预发布代码目录和域名；
- 生产代码、数据库、上传文件的同一时间点快照；
- 数据脱敏/隔离策略；
- 外部短信、支付、通知是否禁用或切沙箱；
- robots/访问控制，防止搜索引擎和真实用户进入；
- 回滚和销毁方法。

复制前先处理可能导致配置或备份公开泄露的风险，不得把生产危险文件原样公开到新域名。

## 4. 生产发布

- 不使用在线编辑器直接批量改 164 个模板。
- 使用可审计发布包、明确文件清单和版本化静态资源。
- 发布前做文件和数据库备份。
- 分批替换并保留上一版本。
- 发布窗口内验证移动浏览器和推广宝 App。
- 异常优先回滚，不在线热修业务逻辑。

## 5. 数据库边界

视觉改造原则上不需要数据库 schema 变更。需要数据库操作时：

- 先说明原因和精确 SQL；
- 先在预发布执行；
- 备份并评估锁表/数据量；
- 资金、积分、订单表不得用真实数据试写；
- 只读调查尽量使用只读账号；
- 任何密码不进入命令历史或文档。

## 6. 已知安全风险

调查中发现的公开备份、运维/测试脚本、日志、宽松权限、旧 TLS/运行时等风险应另建安全任务。视觉任务无权擅自删除或升级，但以下情况会阻断发布：

- 预发布复制会扩大凭据暴露；
- APK/H5 会引用不安全 HTTP；
- 支付需要忽略 TLS 错误；
- 构建产物包含生产配置或签名秘密。

## 7. Android 构建安全

- keystore 存在负责人控制的加密存储，不放服务器 Web 根目录。
- APK 不嵌入网站账号、数据库信息或通用管理 Token。
- 只允许 HTTPS 主域名，外部 scheme 使用白名单。
- Release 禁止 WebView 调试和可导出敏感组件。
- 构建日志、崩溃日志不记录 URL 中敏感参数。
