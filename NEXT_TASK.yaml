schema_version: 1
project_id: TG-H5-UI-REDESIGN
task_id: TASK-R02-001
release: R02
status: IN_PROGRESS
title: 开发推广宝品牌与 H5 公共浅色壳层

preconditions:
  - MANIFEST_SHA256.txt verification passes after the R01 closeout commit
  - R01_RESULT.md conclusion is PASS for development closeout
  - production remains unchanged and all edits occur in an isolated R02 staging copy
  - staging write access is limited to code/assets; business POST and real user data writes remain blocked
  - no credentials cookies tokens or signing secrets enter source evidence or screenshots

read_first:
  - 06_HARD_GATES.md
  - 00_PROJECT_CHARTER.md
  - CURRENT_STATUS.yaml
  - 04_VISUAL_SYSTEM.md
  - 11_BRAND_GUIDE.md
  - STYLE_TOKENS.json
  - 02_PAGE_EXPERIENCE_LOG.md
  - 03_PAGE_LEDGER.csv
  - 12_PAGE_SOURCE_MAP.csv
  - 13_SIDE_EFFECT_TEST_PLAN.md
  - LESSONS_LEARNED.md
  - DECISIONS.md
  - version-results/R01_RESULT.md

scope:
  - DESKTOP-SPLASH
  - shared H5 token stylesheet and versioned local asset foundation
  - shared header footer bottom navigation card button input label empty loading toast and dialog primitives
  - browser safe-area behavior and Android WebView compatibility contract
  - local SVG or subset iconfont foundation with no public CDN dependency

actions:
  - Create a new immutable R02 server staging snapshot from the verified R00 baseline or current production snapshot without editing production.
  - Record staging source hash backup rollback path listener and method restrictions before edits.
  - Inventory the exact shared header footer navigation CSS and icon entry points used by R03 through R09.
  - Implement STYLE_TOKENS.json as a versioned local CSS custom-property layer.
  - Implement the Light Grid shared page shell and components with light backgrounds only.
  - Create a local icon strategy using SVG sprite inline SVG or a verified WOFF2 subset; do not add public CDN dependencies.
  - Redesign DESKTOP-SPLASH as a light 推广宝 H5 guidance page while preserving its mobile redirect behavior.
  - Add browser safe-area CSS without double-applying Android native status-bar insets.
  - Keep legacy pages not yet assigned to R02 visually and functionally stable; shared assets must be opt-in or compatibility-scoped.
  - Capture 360x800 390x844 and 430x932 screenshots plus Android-WebView-safe layout evidence where applicable.
  - Check CSS JS icon font and image requests for 404 mixed content external CDN and cache-header regressions.
  - Compare routes query parameters forms hidden fields and side effects before and after; business behavior must remain identical.
  - Run applicable static screenshot console overflow touch-target contrast and manifest gates.
  - Build a new server-signed Android APK only if R02 H5/App integration requires native source or packaged-resource changes; otherwise record why R01 APK remains current.
  - Put every newly built test APK on the local Desktop; do not wait for owner feedback.
  - Update page ledger source map evidence decisions lessons CURRENT_STATUS NEXT_TASK and manifest.
  - Run the full global drift audit before R02 closeout, then immediately continue to R03.

forbidden:
  - Do not edit /www/wwwroot/tg.suewammes.com in place.
  - Do not change login registration legal finance reward upload payment or other business behavior.
  - Do not mark R03 through R09 pages visually verified merely because they consume shared assets.
  - Do not use online Tailwind Font Awesome Google Fonts or another public runtime UI CDN.
  - Do not add dark page backgrounds large dark surfaces warm-orange primary branding or old 签米 product copy.
  - Do not submit SMS login payment withdrawal purchase reward delete certification or account-cancellation actions.
  - Do not restore GitHub Actions automatic testing.
  - Do not wait for asynchronous owner device feedback before continuing safe documented development.

acceptance:
  - DESKTOP-SPLASH uses the 推广宝 name and approved light visual system while preserving mobile redirect behavior.
  - Shared tokens and components are versioned local assets and do not require Tailwind or a public icon CDN.
  - 360x800 390x844 and 430x932 have no horizontal overflow clipping status-bar overlap or large dark surfaces.
  - Text controls touch targets focus states and contrast meet the frozen gates.
  - Legacy pages outside R02 retain routes parameters forms and business behavior.
  - Browser and Android safe-area paths do not double-offset the header.
  - Runtime asset checks show no new 404 mixed content or blocking public UI CDN.
  - Production code and data remain unchanged.
  - R02_RESULT.md and the global drift audit pass.
  - CURRENT_STATUS.yaml NEXT_TASK.yaml evidence decisions lessons and manifest are synchronized.

on_success:
  next_release: R03
  next_task_title: 开发公共认证与法律信息页面
