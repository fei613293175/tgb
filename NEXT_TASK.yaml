schema_version: 1
project_id: TG-H5-UI-REDESIGN
task_id: TASK-R09-001
release: R09
status: IN_PROGRESS
title: 全站最终复核并准备生产发布

read_first:
  - 06_HARD_GATES.md
  - 00_PROJECT_CHARTER.md
  - CURRENT_STATUS.yaml
  - version-results/R08_RESULT.md
  - evidence/R08/after/R08-V2-FINAL-BROWSER-MATRIX.json
  - 03_PAGE_LEDGER.csv
  - 17_RUNTIME_CLICK_GRAPH.csv
  - 04_VISUAL_SYSTEM.md
  - DECISIONS.md
  - LESSONS_LEARNED.md

scope:
  - run one final regression over all completed real-reachable page families
  - confirm R09 residual plugins remain out of visual scope because they have no visible parent entry
  - verify local assets light UI branding business equivalence rollback and production package readiness
  - resolve only defects found on already in-scope pages

out_of_scope_plugins:
  - xigua_hs
  - xigua_sp
  - tb_jjd
  - tb_cus_adv
  - tb_cus_taojing

actions:
  - assemble the closed R02 through R08 overlays into one production candidate
  - run global static and mobile viewport regression once
  - verify Android container compatibility without rebuilding the APK when native source is unchanged
  - prepare production backup deployment and rollback scripts
  - keep owner device upload payment and safe-area feedback asynchronous

progress:
  candidate_assembled: true
  candidate_files: 81
  candidate_matches_r08_staging: true
  static_release_gates_r02_through_r08: PASS
  production_script_verify_only: PASS
  production_deployed: false

next_action:
  - run the final read-only browser regression across the completed real-reachable page set
  - apply the production candidate only after the global regression and rollback preflight pass

forbidden:
  - do not redesign source-only hidden or direct-URL pages
  - do not change business scripts forms routes values balances claims or payment protocols
  - do not restore GitHub Actions automatic triggers
  - do not deploy production until the complete production candidate and rollback verification pass

acceptance:
  - every visual file belongs to a real-reachable page already approved in R02 through R08
  - no hidden page or dormant plugin is added to the production candidate
  - global mobile matrix has no horizontal overflow overlap clipped controls or dark page background
  - public UI CDN count is zero for redesigned pages
  - production candidate is reproducible and has a verified rollback package
  - R09 result status decisions lessons and manifest are synchronized
