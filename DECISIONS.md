# 决策记录

## D-001 视觉改变、业务完全一致

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：全站可见 UI 可以彻底换色换风格，但业务功能、规则、参数、数据和结果完全一致。
- 后果：任何业务逻辑变化必须另建项目，不得混入 UI 版本。

## D-002 H5 是唯一业务前端

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：站点不开发电脑版业务 UI；桌面入口可保留移动引导，所有业务视觉以 H5 为准。

## D-003 页面只使用浅色主题

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：页面整体背景以浅灰蓝、白色为主，禁止大面积深色。深色只用于文字、描边和小面积图标。
- 被替代：最初的“深海电光”方向被本决定废止。
- 正式方向：`晴空电光 / Light Grid`。

## D-004 平台品牌统一为“推广宝”

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：H5 和 Android App 的产品名、图标、启动页和可控品牌位深度统一为“推广宝”。
- 例外：法律主体、支付商户名和合同正文需负责人/法务确认。

## D-005 Android App 身份

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：使用 Kotlin 原生 WebView 容器；App 名“推广宝”；applicationId `com.suewammes.tuiguangbao`。
- 说明：包名一旦发布不可随意更改。

## D-006 Android 必备能力

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：App 必须支持系统相册/图片上传、支付宝等白名单第三方 App 拉起及返回、状态栏安全区、离线/错误处理。

## D-007 APK 体积下限

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：最终签名 Release APK 不低于 10 MiB（10,485,760 字节），在签名文件上检测并记录 SHA-256。

## D-008 签名安全

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：正式 alias 使用 `tuiguangbao`，RSA 4096；keystore 和密码不进入源码/交接包，由负责人安全保管；后续版本保持证书连续。

## D-009 状态栏安全区由原生管理

- 日期：2026-07-26
- 状态：SUPERSEDED_BY_D-037
- 决定：Android 原生宿主为 WebView 应用系统栏 insets，H5 在 App 内不重复叠加状态栏高度；浏览器环境仍使用 CSS safe-area。

## D-010 品牌图标

- 日期：2026-07-26
- 状态：ACCEPTED_WITH_PRODUCTIZATION_TEST
- 决定：采用 `tuiguangbao-app-icon-master-light-v2.png` 作为源图；需在 R01 拆分自适应图标并完成小尺寸/裁切/商标检查。
- 否决：深色背景 v1 不满足浅色要求，只保留为决策证据。

## D-011 不直接改生产

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：所有开发先进入访问受限的预发布副本；生产仅接收已验证且可回滚的版本。

## D-012 版本顺序

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：R00→R09 严格顺序推进，同一时间只允许一个大版本进行。

## D-013 关版后自动连续开发

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：负责人已经明确授权，第一基线完成后无需逐版重复确认；每个大版本通过硬门禁、关版记录和漂移审计后，立即将唯一状态切换到下一版本并继续开发，直至 R09 全部收尾。
- 边界：此授权不允许跳版、并版、绕过支付/提现/注销等隔离测试门禁，也不允许直接修改生产。

## D-014 R00 舞台采用回环只读模式

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：R00 舞台只监听 `127.0.0.1:18081`，仅允许 `GET`/`HEAD`，通过 SSH 隧道访问；写动作按页面版本临时开启最小白名单窗口。
- 原因：先保证模板盘点和视觉开发不会意外触发真实短信、支付、奖励、删除或用户数据写入。

## D-015 Android 工具链不依赖系统 PATH

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：使用经过官方 SHA-256 校验的免安装 Microsoft OpenJDK 21；Android 工程必须提交 Gradle Wrapper，并由脚本定位 JDK/SDK。
- 原因：本机 Android Studio `jbr` 不完整，系统级安装又受管理员确认限制；工作区工具链更可复现。

## D-016 Android 最终 APK 由服务器隔离环境构建

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：本机用于快速编译、lint 和调试验证；R01 最终签名 APK 必须再由服务器既有 `hhy-android-toolchain:r08-api36-cache` 隔离容器构建，并下载到本机桌面供负责人测试。
- 边界：服务器源码与签名放在 `/opt/tg-android-r01/builds/<build-id>/source` 与同级 root-only `private` 目录，禁止放进 `/www/wwwroot/tg.suewammes.com`；生产站点不参与构建。

## D-017 GitHub Actions 作为独立复验环境

- 日期：2026-07-26
- 状态：ACCEPTED
- 仓库：`https://github.com/fei613293175/tgb`
- 决定：完整无状态交接包和 Android 源码进入该仓库；Actions 自动完成干净编译、APK 打包、Android 36 模拟器安装/启动、页面截图、日志检查和回归。
- 失败策略：编译错误、崩溃、测试失败或明显 UI 偏差必须自行分析、修复、重新打包和重测，直到替代运行全绿；不得弱化门禁换取绿灯。
- 产物边界：Actions 只生成 CI Debug 验证包；正式交付仍使用服务器 RSA 4096 签名 Release。任何签名或站点秘密不得进入 GitHub。

## D-018 Android 页面加载不显示原生进度条

- 日期：2026-07-26
- 状态：ACCEPTED
- 实体机反馈：R01 首包可正常安装和访问，但状态栏下方出现蓝色线性页面加载进度。
- 决定：删除原生顶部进度条及 WebChromeClient 进度回调，WebView 继续默认自动加载；不以其他可视进度条替代。
- 验收：新 APK 启动、刷新、前进/后退和页面跳转时，状态栏下方不再出现原生蓝色细线。

## D-019 Tailwind 与图标资源本地化

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：R02 起的新设计不使用在线 Tailwind CSS。旧页已有在线 Tailwind 在对应页面迁移时移除，不能继续作为新视觉依赖。
- 图标策略：优先本地 SVG sprite/内联 SVG；保留 iconfont 时，从现有授权字体裁出实际使用字符的 WOFF2 子集，部署到本站版本化静态目录并配置长期缓存。禁止为了图标新增公共 CDN。
- 性能门禁：首屏图标不得因外网 CDN 阻塞；记录字体/精灵体积、缓存头、404 和加载时序。

## D-020 停止 GitHub Actions 自动测试，真机反馈异步化

- 日期：2026-07-26
- 状态：ACCEPTED，覆盖 D-017 中“自动运行和关版依赖”的部分
- 负责人决定：从现在起不再使用 GitHub Actions 自动测试；开发完成的服务器正式签名 APK 放到桌面，由负责人亲自测试并异步返回问题，开发期间不等待反馈，继续按文档顺序推进。
- 实施：工作流删除 `push`、`pull_request` 和定时触发，只保留历史 `workflow_dispatch`；后续关版不依赖 Actions。
- 质量边界：本机自动检查和服务器隔离正式构建仍是必做门禁；收到异步反馈必须登记并完成修复、重建、重交付。相册真实上传和第三方支付拉起/返回保留为 R09 生产发布前必须清零的累计实体机门禁。

## D-021 服务器源码归档必须保持 Unix 脚本字节

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：从 Windows 向 Linux 服务器传源码时，归档必须从受 Git 跟踪的工作树文件生成，并在构建前检查 `gradlew` 首行使用 LF；禁止让 PowerShell 文本管道处理 tar 字节流。
- 原因：PowerShell 文本管道生成的首个归档把 `gradlew` 变为 CRLF，服务器容器报“required file not found”。重新以二进制归档、分片传输并核对大小和 SHA-256 后构建通过。

## D-022 支付 H5 采用源码证据驱动的精确域名白名单

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：从本站收银台异步 JavaScript 跳转到生产插件源码中已确认的 HTTPS 支付域名时，允许页面留在 WebView 内；之后只允许从本站或这些支付页拉起 host=`platformapi`、scheme=`alipays|alipay`、package=`com.eg.android.AlipayGphone` 的支付宝。
- 当前精确 host：`api.xunhupay.com`、`fuylink.cy253.top`、`mapi.alipay.com`、`openapi.alipay.com`、`sandcash.mixienet.com.cn`、`wappaygw.alipay.com`。
- 边界：不允许通配后缀、尾随点、userinfo、HTTP 降级或任意 intent。新增域名必须先取得源码或脱敏真实跳转证据、补测试并更新本决定。

## D-023 R02 共享壳层采用本地版本化覆盖层

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：R02 不直接重写 278 KiB 旧 `custom.css`，而是在其后加载
  `static/tgb-r02/light-grid-r02.css`。所有选择器受
  `html.tgb-light-grid` 约束，品牌标志和聊天图标使用本站 SVG。
- 原因：可逐页迁移、可独立回滚、不新增 Tailwind/公共 CDN，且不会把尚未
  分配到 R02 的例外页面误标为已改造。
- 边界：共享覆盖通过不代表 R03-R09 页面通过；内联页面必须在所属版本单独
  检查和迁移。

## D-024 写入型视觉测试只能使用可恢复的临时窗口

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：R02 默认始终为 GET/HEAD-only。确需验证登录时，可在回环监听和
  隔离数据库上临时允许 POST，并固定 Android User-Agent；必须保存原配置、
  `nginx -t`、测试后恢复、确认 POST=405，再运行完整 staging verify。
- 禁止：不得在生产开测试窗口，不得为通过测试重置生产密码，不得把临时
  模式留到下一任务。

## D-025 登录后视觉证据使用 staging-only 合成账号

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：真实负责人登录标识无法从克隆数据验证时，不重置生产密码；只在 R02
  主库与 UCenter 克隆库创建固定命名的合成视觉账号。密码随机生成且不输出，
  浏览器通过回环临时桥建立短时会话。
- 回收：截图后必须移走登录桥、恢复动态 User-Agent、确认 `POST=405`，再运行
  完整 staging 和生产不变性门禁。合成账号可留在隔离库供后续可恢复夹具复用，
  但任何 Web 登录入口不得常驻。
- 边界：合成视觉会话只证明页面渲染，不证明真实登录、资金、上传或写入业务。

## D-026 R02 共享导航图标采用本站 Remixicon 3.5.0

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：11 个实际共享导航模板统一引用本站版本化 Remixicon 3.5.0 CSS/WOFF2；
  `my_new.php`、`vip.php` 的 Font Awesome 引用改为服务器既有本地 4.7.0。
- 门禁：固定 WOFF2 SHA-256、CSS/字体体积、HTTP 内容和逐文件部署哈希；禁止
  恢复 jsDelivr 或其他公共 UI CDN。

## D-027 R02 App 安全区截图转为异步累计设备门禁

- 日期：2026-07-26
- 状态：ACCEPTED_UNDER_D-020
- 决定：R02 H5 的浏览器安全区、原生单一 inset 所有权和 instrumentation 编译
  门禁完成后，可做开发关版并继续 R03；本机 AVD 无法启动造成的 App 截图缺口
  继续列入累计实体机门禁，不等待负责人异步反馈。
- 依据：R02 未修改 Android 正式源码或资源，R01 服务器签名 APK 仍有效；D-020
  与 G25 明确设备反馈异步且开发不得停滞。
- 边界：这不是生产验收豁免。R09 发布前必须取得实体机状态栏/手势区截图，
  发现重叠或双空白时回到当前版本缺陷队列修复并重建正式 APK。

## D-028 R03 认证页采用局部编译 CSS 和既有本地资源

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：三张认证模板保留原表单、字段、隐藏值和业务脚本，只在模板末端加载
  `auth-r03.css` 与 `auth-light-grid-r03.css`；jQuery、Font Awesome、品牌图标均
  使用本站资源，不再调用在线 Tailwind 或公共 UI CDN。
- 原因：认证协议可以逐属性对比，资源可缓存和回滚，同时不扩大到 R04 页面。
- 边界：局部覆盖不能重命名业务选择器、按钮 ID、action 或 SMS 端点。

## D-029 R03 法律页面只改阅读层，不改法律含义

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：六份协议、防诈骗和会员协议只增加本站法律阅读 CSS；帮助/关于允许更新
  可控页面标题和推广宝 Logo。历史 MKT、主体、备案、版权、合同和支付含义逐字冻结。
- 证据：静态门禁从覆盖文件逆向移除允许的 UI 差异后，必须与基线完全相等。
- 边界：B-LEGAL-01 不阻塞视觉关版，但没有负责人/法务书面批准不得改正文。

## D-030 受控认证只在 R03 回环窗口使用临时夹具

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：密码和短信成功/失败测试仅在 R03 克隆库执行；验证码直接注入克隆库，
  禁止调用真实短信。临时 Nginx POST 窗口和夹具必须在同一脚本的 trap 中回收。
- 有效性：数据库断言必须写出独立成功标记文件；Discuz 页面文本或进程退出码不能
  单独作为 PASS。失败运行必须显式 `INVALIDATED`，不得在关版中引用。
- 脱敏：邀请编号、账号、验证码、Cookie 和 formhash 不进入截图或交接包。

## D-031 DOM 存在不等于用户可达功能

- 日期：2026-07-26
- 状态：ACCEPTED
- 触发：R04 曾用 CSS 解除首页零高父容器裁剪，错误显示“为你推荐、首发项目、速赚专区、火爆项目”。这些节点只存在于旧模板 DOM/条件协议，原首页并不可见。
- 决定：页面台账和验收以改前运行时可见、可点击状态为准。零高、`hidden`、`none`、插件开关关闭或条件未满足的节点不得因换色/重构显现，也不得作为新增入口。
- 门禁：改前后记录计算样式、裁剪后暴露面积、`elementFromPoint` 命中、占位高度和截图；静态 CSS 禁止针对旧零高父容器解除裁剪。
- 边界：隐藏条件路由可做只读协议探针，但证据必须标为 `HIDDEN_CONDITIONAL_ROUTES`，不能描述为用户实点。

## D-032 并行子线程采用主线程集成制

- 日期：2026-07-26
- 状态：ACCEPTED
- 决定：允许一个主线程加最多三个子线程并行；子线程只承担页面文件互斥的实现、只读预研、视觉回归或文档审计，主线程独占共享文件、服务器部署、状态切换、manifest 和最终关版。
- 版本边界：当前版本未 PASS 时，下一版本只允许只读预研，不得编辑或标记完成。所有子线程结果必须由主线程用当前工作树和运行时证据复核后合入。
- 冲突边界：`common_header.php`、`common_footer.php`、共享 `app.js`、状态文档和门禁脚本默认禁止子线程并发编辑。

## D-033 UI 范围只由真实前端点击图决定

- 日期：2026-07-27
- 状态：ACCEPTED，覆盖此前将“直接 URL 可访问”或“源码已发现”计入视觉范围的记录
- 负责人纠偏：只美化普通真实用户从原平台前端可见入口逐级点击能够进入的页面。评论中心、发布名片等没有真实前端入口的页面不得因为源码或直接 URL 存在而纳入改造。
- 决定：`16_RUNTIME_CLICK_AUDIT.md` 与 `17_RUNTIME_CLICK_GRAPH.csv` 成为页面范围的强制依据。除根入口外，任何 `IN_SCOPE` 页面都必须有父页面截图、祖先裁剪后暴露面积、命中测试和实际点击到达证据。
- 失效证据：R05 v3 对评论、收藏、背景、名片等直接路由的截图和布局矩阵标记为 `INVALIDATED_SCOPE_DRIFT`，不得支持 R05 PASS。
- 实施：重新从生产 H5 首页按真实用户路径审计；有 GET 副作用的子页面在生产只记录可见入口，再到未改 UI 的隔离基线重放；审计后撤销超范围 UI 覆盖并重算 R05-R09。
- 边界：源码映射仍保留用于保护业务协议和防止公共 CSS 泄漏，但映射存在不等于 UI 开发任务。
- R05 运行时裁定：`CONTENT-DETAIL` 与 `SAFETY-REPORT` 已由生产父入口加隔离实点获得范围；评论、点赞、收藏、红包和名片在详情运行时不可见或零暴露，不得随详情 CSS 一并美化或显现。
- R05 首页纠偏：R04 CSS 曾把零高旧分类容器内的子元素强制显示；R05 v5 以 12 文件范围包撤销该漂移，三视口浏览器矩阵和隐藏控件门禁通过，旧分类继续保持非入口、非视觉范围。

## D-034 后续 UI 开发采用三门禁快速批处理

- 日期：2026-07-27
- 状态：ACCEPTED，适用于 R07-R09 及返修
- 决定：普通页面换色只保留三项必要门禁：入口必须真实可达、业务协议必须不变、部署必须可回滚。互斥页面文件允许并行开发，由主线程一次合并、一次部署、一次整批回归。
- 边界：资金、支付、上传、奖励和删除等高风险操作仍不得在真实账户提交；该简化取消重复文档和重复截图，不取消安全边界。

## D-035 R09 上线与后续返修采用轻量闭环

- 日期：2026-07-27
- 状态：ACCEPTED，细化 D-020、D-034
- 负责人要求：项目不再复杂化，目标就是把每个真实可达前端页面换配色和 UI 样式，功能完全不变；负责人真机反馈异步返回，开发不等待。
- 决定：R09 候选只执行一次 `390x844` 全局快速回归、静态协议门禁、生产哈希和回滚预检。已通过历史三视口且本批未改动的页面不重复截图或重复执行高风险业务动作。
- 上线边界：本次只发布 H5 视觉文件，Android 源码、支付协议、上传协议和业务脚本未改变，因此 H5 可先上线；相册上传、支付宝拉起/返回和安全区真机反馈继续作为异步缺陷输入，不再阻塞纯视觉部署。
- 返修节奏：收到截图或问题后，定位真实页面模板，直接修复，跑受影响页面和一个共享回归，备份后发布；不新建无必要的大版本、候选矩阵或重复证明材料。

## D-036 APK 只由负责人真机测试，H5 仍由 Codex 浏览器验收

- 日期：2026-07-27
- 状态：ACCEPTED，覆盖此前要求 Codex 本机/服务器模拟器或 APK 功能测试的记录
- APK 边界：禁止在负责人电脑安装 Android SDK、ADB、模拟器、系统镜像或相关测试环境；禁止 Codex 安装、启动或功能测试 APK，APK 真机测试全部由负责人完成。
- Codex Android 职责：只开发源码；原生源码变化时使用服务器现有构建环境生成正式签名 APK，完成体积、包名、签名和 SHA-256 检查，然后复制到桌面。
- H5 边界：页面设计完成后，Codex 必须自己用浏览器逐页打开体验，并通过截图人工比对变形、错位、溢出、遮挡、品牌和浅色视觉；“不测试 APK”不能成为跳过 H5 浏览器验收的理由。
- 反馈闭环：负责人异步返回 APK 截图或问题后，Codex 直接分析、修复、重新构建并交付新的桌面 APK，不代替负责人进行真机测试。
- 当前事实：2026-07-27 本机未发现 Android SDK、ADB 或模拟器进程；本轮 R09 仅改 H5，Android 生产源码未变化，因此继续使用现有服务器签名 APK，不重建。

## D-037 WebView 穿透系统栏，H5 单一负责页面安全区

- 日期：2026-07-27
- 状态：ACCEPTED，覆盖 D-009、L-011 和旧版 G18 中“原生根容器拥有系统栏 inset”的实现规则
- 负责人真机反馈：所有 App 页面在状态栏下方出现过高空白；旧 App 的 WebView 可铺到状态栏后方，新 App 又在根容器添加 `statusBars.top`，与 H5 顶栏和 `safe-area-inset-top` 叠加。
- 决定：保留 `WindowCompat.setDecorFitsSystemWindows(window, false)`；移除原生根容器 WindowInsets listener、四边 padding 和 inset 消费。WebView 从窗口 `top: 0` 开始，H5 继续单一负责各页面安全区。
- 视觉边界：浅色顶栏背景允许延伸到状态栏后方；不得再出现状态栏下第二段原生空白。关键标题、返回键和输入控件是否被具体机型系统图标遮挡，由负责人真机截图异步验收并按当前 R09 快速返修。
- 验证边界：Codex 不安装或运行 APK；服务器只执行单元测试、Release Lint、签名、包名、SDK、体积和 SHA-256 门禁。H5 浏览器 `390x844` 首页顶栏 `68.8px`、签到页顶栏 `56px`，均无横向溢出。

## D-038 页面体验只接受 Android H5 UA

- 日期：2026-07-27
- 状态：ACCEPTED，补强 G28/G30
- 负责人提醒：本站不支持电脑版，必须使用 H5 UA 体验。
- 决定：以后所有范围审计、页面体验、截图和 UI 结论统一使用 Android mobile UA；
  App 场景追加 `TuiGuangBaoAndroid/1.0.0`。桌面 UA 结果一律不作为页面证据。
- 本轮纠偏：公开登录链、搜索新标签、防诈骗、设置和个人中心均按 Android H5 UA
  重新核验。会员协议、帮助、关于在真实父页无入口，移出视觉范围并恢复上线前文件。
## D-039 未取得浏览器截图的 H5 覆盖必须回滚

- 决定：页面模板和静态协议门禁通过仍不等于视觉验收。若真实 Android H5 浏览器因工具、安全策略或会话错误无法完成截图与几何检查，隔离部署必须恢复到改前哈希，状态写为 `PENDING_BROWSER_VERIFY`。
- 原因：本项目明确要求 Codex 亲自检查变形、错位、溢出和遮挡；curl、HTTP 200、CSS 静态检查和截图文件存在都不能替代肉眼与几何验收。
- 落地：R09 私信/成员四文件覆盖部署 `20260727T123509+0800` 在浏览器受阻后已回滚，夹具和桥全部移除。下一任务从新浏览器会话重新部署，不重新做页面考古。
