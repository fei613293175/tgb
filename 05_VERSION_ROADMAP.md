# 05 版本开发路线

任何版本必须按顺序完成。除非负责人形成书面决策，不得跳版、并版或同时开发两个大版本。

## R00：事实、页面与预发布基座

状态：`COMPLETE`

目标：

- 校验生产事实、插件状态和页面台账；
- 建立不包含凭据的可回滚预发布副本；
- 保留旧 41 类观察和 64 行源码映射作为历史材料；2026-07-27 起 UI 范围必须由真实点击图重新冻结；
- 为会改变数据的交互建立隔离账号、测试内容和账务对账方案；
- 处理会阻断安全复制的高风险暴露决定；
- 冻结“推广宝”品牌名、包名和视觉令牌。

退出条件：

- 预发布 URL、代码/数据库快照 ID、恢复命令可验证；
- 生产未被直接修改；
- 台账每行有负责人、版本和证据状态；
- 交互型页面明确可执行的测试数据；
- 本交接包校验和通过。

## R01：推广宝 Android 原生容器

状态：`COMPLETE`

目标：

- Kotlin 原生 App，应用名“推广宝”；
- 包名 `com.suewammes.tuiguangbao`；
- 生成并适配启动图标、自适应图标、启动页；
- WebView 加载正式/预发布 H5；
- 状态栏和导航栏安全区正确，H5 顶栏不重叠；
- 支持 H5 文件选择和相册图片上传；
- 支持支付宝及经白名单批准的第三方 App 拉起和支付返回；
- 支持返回键、下载、离线、SSL/网络错误、外部链接；
- Release APK 文件体积不低于 10 MiB；
- 形成可持续的签名、版本号和 APK 交付流程。

退出条件：

- 开发关版：本机测试/Lint、服务器正式签名构建、包信息/权限/体积/签名/SHA-256
  门禁通过，APK 已放到桌面；
- 异步累计项目门禁：真机冷启动、登录、上传图片、外部支付沙箱拉起/返回通过；
  D-020/D-035 允许该反馈不阻塞 R02-R09 开发和未改原生协议的纯 H5 视觉部署，但项目最终负责人验收前必须清零；
- APK `>= 10,485,760` 字节；
- 包名、版本、签名证书指纹、SHA-256 入关版记录；
- 无明文凭据、无任意 scheme 拉起、无不安全 SSL 绕过；
- 测试 APK 交付给负责人。

## R02：品牌和公共壳层

状态：`COMPLETE`

- 推广宝品牌字标、页面标题、favicon/站点图标、桌面 H5 引导页；
- 全局 CSS 令牌、页面壳、状态栏安全区、头部、底部导航；
- 按钮、输入、卡片、标签、空态、加载、Toast、Dialog；
- 本地化字体/图标依赖，移除生产 Tailwind/Font Awesome CDN 依赖。

## R03：公共认证与法律信息

状态：`COMPLETE`

- 登录、注册、短信登录；
- 用户/服务/隐私/消费/会员协议、防诈骗、帮助、关于；
- 修复窄列竖排、`$ is not defined` 和长文阅读；
- 品牌文案统一为“推广宝”，但法律正文变更需负责人/法务确认。

## R04：首页与发现

状态：`PASS`

- 首页、搜索、分类、标签、列表、分页、空态；
- 超级头条展示区和发布入口；
- 底部导航与 App WebView 协同；
- 控制台错误清零，不改变列表排序或曝光逻辑。
- 只改原站运行时可见入口；源码中零高、隐藏或条件未启用的 DOM 不得因视觉 CSS 显现。

## R05：内容与发布

状态：`PASS`（2026-07-27，真实点击范围纠偏和五类页面开发完成）

- 当前已发现真实父入口候选：内容详情、发布、我的项目、项目审核；必须完成实际点击或隔离重放后才能冻结范围；
- 详情内部的成员主页、举报、私信、红包等只在原 UI 真实显示且可点击时进入；隐藏的评论、点赞等代码不进入；
- 评论中心、收藏、背景选择、名片新增和名片上传的旧直接 URL 证据全部失效；没有真实父入口时撤销其 UI 覆盖；
- 先重建 R05 点击图、撤销超范围改造，再处理真实可达页的长标题、长图、联系方式和副作用。

## R06：账户与沟通（已完成）

- 状态：`PASS`；最终范围为 8 个点击图已证明页面。
- 个人中心、设置、资料、地址、联系方式、认证、注销确认；
- 消息列表、聊天详情、文件/图片选择、相关弹层；
- 统一旧 Discuz 提示页；
- App 相册选择与聊天/发布上传端到端验证。

## R07：资金、会员与推广购买（已完成）

- 状态：`PASS`；R07 关版时最终范围为 7 个点击图已证明页面，订单和收银台当时均未获范围。R09 后续由会员购买链只补证收银台，订单仍无可见父入口。
- 双钱包、资金明细、提现、订单；
- VIP、刷新套餐、头条、超级头条；
- 支付收银台与 App 第三方拉起/返回；
- 405/406px 溢出修复；
- 金额、积分、订单和回调必须做前后对账。

## R08：增长与奖励（已完成）

- 状态：`PASS`；仅开发邀请、团队、签到、匹配分红和站内 App 下载落地 5 个真实可达页面。
- 邀请、海报、团队；
- 签到、匹配分红、领取、任务；
- 双钱包奖励归属和状态语义；
- 只使用隔离测试数据执行领取类动作。

## R09：剩余插件与全局关版

- 状态：`DEVELOPMENT CLOSEOUT PASS / OWNER VERIFICATION PENDING`；H5 已于 2026-07-27 部署生产并完成 39/39 非 Android 视觉条目关版，当前只接收异步真机反馈并快速返修。
- 只处理真实点击图证明可达的剩余插件；源码存在或插件启用但无前端入口的 `xigua_hs`、`xigua_sp`、`tb_jjd`、广告、淘金等不得改 UI；
- 全站路由覆盖、视觉回归、功能等价、无障碍、性能、资源和安全检查；
- Android 多机型/多 WebView 版本回归；
- 分批生产发布、监控、回滚演练；
- 将所有 `IN_SCOPE` 台账项关闭。

R09 后续补证：会员 VIP 页的可见“立即开通 -> 确认购买”链已在隔离夹具实点进入收银台；仅 `FINANCE-PAYMENT` 获得范围。两文件 Light Grid 覆盖于 `20260727T152930+0800` 部署生产，三视口实拍通过；`FINANCE-ORDERS` 与外部支付页没有因此继承范围，Android 真机支付宝拉起/返回仍是异步门禁。

R09 及后续返修按 D-034/D-035 执行轻量流程：历史三视口基线不重复跑，默认只做一次 `390x844` 主视口回归、静态协议检查、备份和回滚预检。只有共享布局或断点被修改时才补跑边界视口。

R09 是当前路线图最后一个大版本，不存在预设 R10。R09 开发关版后的唯一工作是负责人异步验证安卓安全区、相册上传和支付宝拉起/返回，以及由真实反馈触发的定向返修；不得借此重新开发隐藏页面。

## 每版固定流程

1. 运行 `continuity.ps1 -Mode resume`。
2. 读取硬门禁、状态、下一任务、页面台账、踩坑和决策。
3. 做本版本改前截图、DOM/表单/请求基线。
4. 从生产父页面重放真实点击链；没有点击图父边的页面立即移出视觉范围。
5. 只改本版本真实点击范围。
6. 执行 `07_TEST_MATRIX.md` 适用门禁。
7. 形成关版记录、改后证据、回滚证明。
8. 执行 `continuity.ps1 -Mode drift-audit`。
9. 对照全局文档检查需求、硬门禁、进度、优点复用和踩坑。
10. 若发现漂移，先更新并加固规则，再重新测试。
11. 更新状态、下一任务、台账、点击图、决策、踩坑和校验和。
12. 关版硬门禁 PASS 后自动进入下一版；只在紧急负责人决定阻断时暂停受影响项。
13. 活跃开发每 30 分钟和每个阶段边界记录一次实质产出；连续两次没有新产出时必须切换方法并立即执行下一具体动作，不得原地重复分析。

并行开发只允许页面文件互斥的子线程执行。主线程独占共享模板/脚本、舞台部署、状态文档、
manifest、版本切换和最终关版；子线程不得提前修改后续版本或把只读预研写成完成状态。
