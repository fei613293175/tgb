# 05 版本开发路线

任何版本必须按顺序完成。除非负责人形成书面决策，不得跳版、并版或同时开发两个大版本。

## R00：事实、页面与预发布基座

状态：`COMPLETE`

目标：

- 校验生产事实、插件状态和页面台账；
- 建立不包含凭据的可回滚预发布副本；
- 冻结 41 个已观察页面和其余源码页面的模板映射；
- 为会改变数据的交互建立隔离账号、测试内容和账务对账方案；
- 处理会阻断安全复制的高风险暴露决定；
- 冻结“推广宝”品牌名、包名和视觉令牌。

退出条件：

- 预发布 URL、代码/数据库快照 ID、恢复命令可验证；
- 生产未被直接修改；
- 台账每行有负责人、版本和证据状态；
- 交互型页面明确可执行的测试数据；
- 本交接包校验和通过。

## R01：推广宝 Android 原生容器

状态：`COMPLETE`

目标：

- Kotlin 原生 App，应用名“推广宝”；
- 包名 `com.suewammes.tuiguangbao`；
- 生成并适配启动图标、自适应图标、启动页；
- WebView 加载正式/预发布 H5；
- 状态栏和导航栏安全区正确，H5 顶栏不重叠；
- 支持 H5 文件选择和相册图片上传；
- 支持支付宝及经白名单批准的第三方 App 拉起和支付返回；
- 支持返回键、下载、离线、SSL/网络错误、外部链接；
- Release APK 文件体积不低于 10 MiB；
- 形成可持续的签名、版本号和 APK 交付流程。

退出条件：

- 开发关版：本机测试/Lint、服务器正式签名构建、包信息/权限/体积/签名/SHA-256
  门禁通过，APK 已放到桌面；
- 异步累计发布门禁：真机冷启动、登录、上传图片、外部支付沙箱拉起/返回通过；
  D-020 允许该反馈不阻塞 R02-R09 开发，但生产发布前必须清零；
- APK `>= 10,485,760` 字节；
- 包名、版本、签名证书指纹、SHA-256 入关版记录；
- 无明文凭据、无任意 scheme 拉起、无不安全 SSL 绕过；
- 测试 APK 交付给负责人。

## R02：品牌和公共壳层

状态：`IN_PROGRESS`

- 推广宝品牌字标、页面标题、favicon/站点图标、桌面 H5 引导页；
- 全局 CSS 令牌、页面壳、状态栏安全区、头部、底部导航；
- 按钮、输入、卡片、标签、空态、加载、Toast、Dialog；
- 本地化字体/图标依赖，移除生产 Tailwind/Font Awesome CDN 依赖。

## R03：公共认证与法律信息

- 登录、注册、短信登录；
- 用户/服务/隐私/消费/会员协议、防诈骗、帮助、关于；
- 修复窄列竖排、`$ is not defined` 和长文阅读；
- 品牌文案统一为“推广宝”，但法律正文变更需负责人/法务确认。

## R04：首页与发现

- 首页、搜索、分类、标签、列表、分页、空态；
- 超级头条展示区和发布入口；
- 底部导航与 App WebView 协同；
- 控制台错误清零，不改变列表排序或曝光逻辑。

## R05：内容与发布

- 详情、成员主页、评论、收藏、红包明细；
- 发布/上传/编辑/我的发布/审核/举报/名片；
- 修复长标题、长图、联系方式、486px 溢出；
- 用隔离数据验证新增、修改、删除和副作用。

## R06：账户与沟通

- 个人中心、设置、资料、地址、联系方式、认证、注销确认；
- 消息列表、聊天详情、文件/图片选择、相关弹层；
- 统一旧 Discuz 提示页；
- App 相册选择与聊天/发布上传端到端验证。

## R07：资金、会员、订单与推广购买

- 双钱包、资金明细、提现、订单；
- VIP、刷新套餐、头条、超级头条；
- 支付收银台与 App 第三方拉起/返回；
- 405/406px 溢出修复；
- 金额、积分、订单和回调必须做前后对账。

## R08：增长与奖励

- 邀请、海报、团队；
- 签到、匹配分红、领取、任务；
- 双钱包奖励归属和状态语义；
- 只使用隔离测试数据执行领取类动作。

## R09：剩余插件与全局关版

- 运行时可达的 `xigua_hs`、`xigua_sp`、`tb_jjd`、广告、淘金等残余页面；
- 全站路由覆盖、视觉回归、功能等价、无障碍、性能、资源和安全检查；
- Android 多机型/多 WebView 版本回归；
- 分批生产发布、监控、回滚演练；
- 将所有 `IN_SCOPE` 台账项关闭。

## 每版固定流程

1. 运行 `continuity.ps1 -Mode resume`。
2. 读取硬门禁、状态、下一任务、页面台账、踩坑和决策。
3. 做本版本改前截图、DOM/表单/请求基线。
4. 只改本版本范围。
5. 执行 `07_TEST_MATRIX.md` 适用门禁。
6. 形成关版记录、改后证据、回滚证明。
7. 执行 `continuity.ps1 -Mode drift-audit`。
8. 对照全局文档检查需求、硬门禁、进度、优点复用和踩坑。
9. 若发现漂移，先更新并加固规则，再重新测试。
10. 更新状态、下一任务、台账、决策、踩坑、校验和。
11. 负责人确认后才可开始下一版。
