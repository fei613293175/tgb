# 07 测试与验收矩阵

## 1. 页面级固定证据

每个页面至少保存：

- 页面 ID、路由模板、入口路径和权限；
- 源模板/PHP/CSS/JS 映射；
- 改前/改后同视口截图；
- DOM 中表单协议、隐藏字段和事件绑定 diff；
- 改前隐藏/零高/条件节点的计算样式、祖先裁剪后暴露面积、占位高度和命中测试；
- 网络请求方法、URL、参数名、响应结构对比；
- 成功、失败、空态、加载、权限不足状态；
- 控制台错误和资源 404；
- 360、390、430px 溢出检测；
- 所属版本、提交/快照、测试人和日期。

## 2. 通用视觉矩阵

| 门禁 | 360×800 | 390×844 | 430×932 | App 真机 |
|---|---:|---:|---:|---:|
| 无横向滚动 | 必测 | 必测 | 必测 | 必测 |
| 无窄列竖排 | 必测 | 必测 | 必测 | 必测 |
| 状态栏/安全区 | 抽测 | 必测 | 抽测 | 必测 |
| 底部导航/手势区 | 必测 | 必测 | 必测 | 必测 |
| 输入键盘遮挡 | 必测 | 必测 | 必测 | 必测 |
| 44px 触控目标 | 必测 | 必测 | 必测 | 必测 |
| 浅色主题且无深色大块 | 必测 | 必测 | 必测 | 必测 |
| 文本对比 | 自动+人工 | 自动+人工 | 自动+人工 | 人工 |
| 隐藏节点不显现/不命中/不占位 | 必测 | 必测 | 必测 | 必测 |

## 3. 功能等价矩阵

### 认证

- 密码正确/错误、未填写、锁定；
- 短信发送、倒计时、错误验证码、过期验证码；
- 注册成功/重复手机号/协议未勾选；
- 登录会话在浏览器和 App 中保持。

### 内容

- 搜索有结果/无结果；
- 分类、筛选、分页、返回位置；
- 详情正常/删除/无权/长标题/长图；
- 发布上传、字段校验、成功、审核、驳回；
- 收藏、评论、举报、名片上传。

### 账户

- 资料、头像、地址、默认地址；
- 认证状态；
- 消息列表、聊天文本/图片、发送失败；
- 注销仅在专用隔离账号执行。

### 资金和交易

- 双钱包金额/明细；
- 提现余额不足/待审/失败/成功；
- 订单未付/已付/取消/关闭；
- VIP、刷新、头条购买；
- 支付拉起、取消、完成、回调、重复返回；
- 每个动作前后核对订单、钱包、积分、日志。

### 奖励

- 签到未签/已签；
- 可领取/已领取/过期；
- GET 浏览是否产生奖励；
- 邀请码、团队层级和海报。

## 4. Android 专项

| 类别 | 必测项 |
|---|---|
| 安装 | 全新安装、覆盖升级、签名一致、APK >=10 MiB |
| 启动 | 冷启动、热启动、进程重建、离线 |
| WebView | Cookie、JS、DOM Storage、后退、刷新、外链 |
| 状态栏 | 打孔/刘海、手势导航、浅色系统栏、无重叠 |
| 相册 | 允许/拒绝、单图/多图、取消、大图、不同 Android 版本 |
| 上传 | 发布、头像、名片、聊天四入口真实成功 |
| 支付 | 有/无支付宝、取消/完成、返回、订单刷新 |
| 安全 | 未知 scheme 拒绝、SSL 错误拒绝、非白名单域名外部浏览 |
| 资源 | 无 CDN 新依赖、无敏感信息、无 404 |

## 5. 品牌与浅色门禁

- 所有可控品牌位为“推广宝”；
- 不把法律主体误替换成产品名；
- 页面背景以 `#F4F7FB`、白色和相邻浅色为主；
- 不存在大面积深蓝/黑色头部、卡片、启动页；
- 暖橙/奶油色不再作为主体视觉；
- Logo 使用浅色正式版，不使用被否决的深色草案。

## 6. 性能预算

基线在 R00/R02 实测后冻结。最低要求：

- 新视觉不显著增加首屏阻塞资源；
- 图片按显示尺寸压缩，避免大图直接做背景；
- 不新增运行时公共 CDN；
- 首屏 CSS/JS 可缓存并版本化；
- Android WebView 冷启动、首页可交互时间记录改前/改后；
- 页面内存泄漏、重复事件绑定和重复弹层不得增加。

## 7. 回滚测试

- 预发布部署前后可切换；
- 数据库无视觉版本迁移时不得产生 schema 回滚负担；
- 生产静态资源带版本号，旧页面仍能取到匹配资源；
- Android 可重新安装上一签名 APK；
- 回滚后登录、首页、详情、上传和支付冒烟通过。

## 8. 测试失败处理

任一 MUST 门禁失败：

1. 停止该版本发布；
2. 写入 `LESSONS_LEARNED.md` 或缺陷记录；
3. 修复并重新执行受影响矩阵；
4. 如果原因是规则缺失，先更新门禁/脚本；
5. 不以负责人“口头看起来可以”替代证据。

## 9. Android 验证与异步反馈

| 环境 | 责任 | 允许作为正式包 |
|---|---|---:|
| 本机 | 快速单元测试、Lint、Debug/Release 预检和修改反馈 | 否 |
| 服务器隔离容器 | 正式签名 Release、体积、签名、包信息和 SHA-256 | 是 |
| 负责人实体机 | 安装、页面体验、相册上传、第三方 App 拉起/返回；反馈异步返回 | 否 |

自 D-020 起不再自动运行 GitHub Actions。每个可测试版本先完成本机门禁和服务器正式签名构建，再把 APK 放到桌面；开发继续推进，不等待负责人反馈。任何异步反馈到达后必须登记、修复并重新生成桌面 APK。截图仍需人工确认浅色、品牌、状态栏无重叠、无空白/崩溃/SSL 页；相册选择、真实上传和第三方支付 App 拉起/返回最终必须由实体机证据关闭。
