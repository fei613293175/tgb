# 01 现状事实基线

本文件记录 2026-07-26 的只读调查结果。后续开发前应重新验证易变化事实，不能把本快照当成永久线上状态。

## 1. 运行环境

| 项目 | 已确认事实 |
|---|---|
| 站点 | `https://tg.suewammes.com` |
| 生产根目录 | `/www/wwwroot/tg.suewammes.com` |
| 插件目录 | `/www/wwwroot/tg.suewammes.com/source/plugin` |
| 程序 | Discuz! X3.4，Release 20220518 |
| PHP | 7.4.33 |
| MySQL | 5.7.44 |
| Nginx | 1.26.3 |
| 数据库标识 | 从生产配置只读获取；因其当前与密码字符串重合，不写入交接包 |
| 表前缀 | `pre_` |
| SSH 连接入口 | 本机别名 `obx-test`，使用前必须重新 `ssh obx-test` 验证 |

密码、账号、私钥、Cookie 和验证码不属于文档事实，必须从负责人批准的安全渠道获取。

## 2. H5 入口机制

- 根目录 `index.php` 当前将入口硬编码到 `https://tg.suewammes.com/plugin.php?id=xigua_hb`。
- `xigua_hb` 使用移动端检测：桌面 UA 显示二维码/引导页，移动 UA进入 touch 模板。
- 站点本身没有可作为业务主站的电脑版前端；本项目以 H5 为唯一业务 UI。
- Discuz 默认新移动版未启用，`comiis_app` 未启用。
- H5 核心模板位于 `source/plugin/xigua_hb/template/touch/`，观察到 164 个 PHP 模板文件。
- 主界面大量使用 WeUI、jQuery 和插件自定义 CSS；公开登录/注册页还加载 Tailwind CDN 与 Font Awesome。
- 当前主要内容宽度约 640px，但部分页面在 390px 仍产生 405、406、486px 横向宽度。

## 3. 插件事实

- 数据库插件记录：69。
- 数据库启用插件：32。
- 磁盘插件目录：62。
- 启用但磁盘目录缺失：7 个：`creditcount`、`tb_xuanshang`、`bphp_prize`、`duobao`、`mart`、`shangrenfh`、`lianghao`。
- 磁盘存在但数据库未注册：24 个。
- 数据库停用但磁盘仍存在：13 个。

已确认与 H5 用户业务高度相关的插件包括：

`xigua_hb`、`tb_cus_mobilereg`、`tb_pay`、`xigua_hh`、`tb_cus_xiguahh`、`xigua_hs`、`xigua_sp`、`xigua_lt`、`xigua_member`、`xigua_hj`、`xiaomy_certification`、`tb_cus_card`、`tb_cus_taojing`、`tb_cus_adv`、`tb_cus_crashfh`、`tb_jjd`、`view`、`tb_credit`、`tb_toutiao`。

插件启用不代表当前页面可从运行时 UI 进入。必须同时记录“源码发现”和“运行时到达”两种证据。

## 4. 数据规模快照

| 项目 | 约数 |
|---|---:|
| 数据表 | 680 |
| 数据库体积 | 103.5 MB |
| 用户 | 5,519 |
| 主题 | 1,874 |
| 帖子 | 2,589 |
| 项目/发布 | 1,303 |
| 支付记录 | 6,567 |
| 查看日志 | 475,000 |
| 评论 | 10,600 |
| 资金日志 | 28,800 |

这些数据说明页面视觉改造必须充分考虑长列表、空列表、历史数据格式、异常长标题、旧图片和分页，不允许只用理想假数据开发。

## 5. 已观察视觉基线

- 主色：暖橙（常见约 `#FF6B35` / `#FF7B00`）。
- 背景：奶油白（观察到约 `#FFF9F5`）。
- 卡片：白色、大圆角、较多胶囊按钮。
- 字号：主体约 14—16px，页面间缺少统一尺度。
- 圆角：约 15—30px，页面间不一致。
- 公共、插件、旧 Discuz 模板并存，风格断裂明显。

## 6. 已确认缺陷

- 注册、短信登录、服务协议、隐私政策在 390px 出现极窄列或逐字竖排。
- 注册和短信登录控制台出现 `ReferenceError: $ is not defined`。
- 首页控制台观察到 JavaScript 语法错误及空对象样式访问错误。
- 部分页面横向溢出：提现约 405px、刷新套餐约 406px、名片上传约 486px。
- `tb_credit` 联系资料页回退到旧 Discuz“提示信息”外观。
- 隐私政策标题/内容仍出现 `MKT`，可能为历史品牌文案；文案修改不属于视觉任务，需负责人另行确认。
- Tailwind 浏览器 CDN 在生产页给出不建议生产使用的警告。

## 7. 业务副作用事实

内容详情页仅通过 GET 打开就出现“红包已自动放入您的钱包内”，同时可能写入浏览记录、增加浏览数或触发奖励。因此：

- 页面抓取不是天然只读；
- 任何带奖励、访问日志、领取、红包或计数的 GET 页面都必须纳入副作用矩阵；
- 自动化不能盲目全站爬取；
- 必须使用隔离测试账号、测试内容和前后数据对账。

## 8. 独立安全风险记录

只读调查发现可能存在公开 ZIP 备份、运维脚本、`phpinfo`、日志/测试文件、宽松权限、TLS 1.1 和旧运行时等风险。它们与视觉重构不是同一任务。

处理规则：

1. 不在视觉任务中擅自删除或升级；
2. 在生产改造前由负责人单独做安全处置决定；
3. 若风险会暴露凭据或阻断预发布复制，必须先暂停受影响任务；
4. 安全修复应另建任务、备份和回归，不混入 UI 版本。
