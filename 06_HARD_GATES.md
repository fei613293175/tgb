# 06 全局硬性门禁

任何 AI、开发者、脚本或版本都必须遵守。`MUST` 失败即停止受影响任务，不允许用“之后再补”绕过。

## A. 凭据和生产

### G00 凭据零落盘（MUST）

交接包、源码、提交、日志、截图和 APK 内不得包含账号、密码、数据库密码、私钥、Cookie、Token、验证码、formhash、签名密码或真实身份资料。凭据只通过负责人批准的外部安全渠道提供。

### G01 禁止生产试改（MUST）

没有预发布验证、备份 ID、恢复命令和负责人批准，不得修改 `/www/wwwroot/tg.suewammes.com`。先在预发布副本开发。

### G02 可恢复（MUST）

每次发布前必须有代码、数据库、上传资源的匹配快照和恢复演练。回滚命令不得依赖未记录的聊天信息。

## B. 业务功能完全一致

### G03 路由/表单协议冻结（MUST）

不得改变：

- URL、插件 `id`、`ac`、`modac` 及其他查询参数名和含义；
- form action、method、字段 name/value、隐藏字段、`formhash`；
- PHP 模板变量、Discuz hook、AJAX 请求/响应形状；
- Intent/支付回调参数和订单关联字段。

允许调整 DOM 时必须保存改前/改后协议 diff，证明业务控件仍存在。

### G04 业务规则冻结（MUST）

不得改变权限、金额、积分、红包、奖励、分红、审核、排序、曝光、提现、支付、注册、认证、注销逻辑；不得改数据库结构、插件启停或后台配置。

### G05 功能等价证据（MUST）

“页面可打开”不等于通过。每个页面必须验证原入口、原动作、成功/失败/空态/权限态及服务端结果，资金类还需账务前后对账。

## C. 页面覆盖和数据副作用

### G06 页面台账（MUST）

所有可点击、条件可达、弹层、结果页和插件页面都必须进入 `03_PAGE_LEDGER.csv`。不得只改常用首页。

### G07 GET 副作用隔离（MUST）

详情查看等 GET 可能写浏览、奖励或钱包数据。自动化只允许使用隔离账号和测试内容，并记录前后数据库差异。

### G08 高风险动作授权（MUST）

支付、购买、提现、签到、领取、发红包、发消息、发布、删除、举报、实名、验证码、退出、注销等动作，只能在明确测试用例和隔离数据下执行。生产真实账号不得用于破坏性验证。

## D. H5 视觉与兼容

### G09 视口和安全区（MUST）

360×800、390×844、430×932 均不得横向滚动、窄列竖排、遮挡或不可点。顶部必须同时适配浏览器与 Android App 状态栏，底部适配手势安全区。

### G10 可访问性（MUST）

触控目标至少 44×44px；输入字体至少 16px；正文对比 4.5:1；焦点可见；状态不得只靠颜色；动态内容有可理解反馈。

### G11 本地资源（MUST）

新 UI 运行时不得依赖新增公共 CDN。新设计不得继续调用在线 Tailwind CSS；Tailwind/Font Awesome 等现有 CDN 应在相应版本迁移为本地、可锁定资源。图标优先使用本地 SVG sprite/内联 SVG；需要保留 iconfont 时，只发布实际使用字符的 WOFF2 子集并由本站静态资源服务器长期缓存。不得引入来源不明资产，图标不可因第三方网络失败而消失。

### G12 旧 WebView 兼容（MUST）

不为视觉效果引入未经验证的新框架、重型动画或仅新浏览器支持的关键能力。必须在 Android 系统 WebView 和微信内置浏览器真机验证。

## E. 推广宝品牌

### G13 品牌统一（MUST）

App 名、H5 页面标题、登录/注册、桌面引导、启动页、图标、关于页和可控站内品牌显示统一为“推广宝”。涉及法律主体、合同正文、支付商户名的文字不能擅改，必须由负责人确认。

### G14 新视觉断代（MUST）

新页面必须使用 `STYLE_TOKENS.json` 的“晴空电光”浅色体系，不得继续以暖橙、奶油底、大胶囊作为主体风格，也不得使用大面积深色背景、深色头部或深色启动页。深色仅限文字、描边和小面积图标。业务语义色不受装饰偏好覆盖。

## F. Android App

### G15 原生容器身份（MUST）

- App 名：`推广宝`
- applicationId：`com.suewammes.tuiguangbao`
- 主域名：`tg.suewammes.com`
- 只允许经过配置的 HTTPS 主机在 WebView 内打开。

变更包名属于破坏性产品决定，必须新建书面决策。

### G16 相册与上传（MUST）

优先使用系统 Photo Picker/Storage Access Framework，按 Android 版本请求最小权限。不得申请无必要的全盘存储权限。H5 `input[type=file]` 的 MIME、单/多选、取消和回调必须完整保留。

### G17 第三方 App 拉起（MUST）

只允许白名单 scheme/intent（包括支付宝实际支付链路）。解析失败时回退到安全 H5 或明确错误；禁止任意 scheme、`javascript:` 外跳和忽略 SSL 错误。必须验证 App→支付宝→App/H5 返回与订单状态刷新。

### G18 状态栏不重叠（MUST）

原生端统一处理 edge-to-edge/insets，H5 使用协商后的 `safe-area-inset-top`。不得同时重复加高或完全不加，浏览器和 App 两种环境分别截图验证。

### G18A 无可视化页面加载进度（MUST）

推广宝 Android 容器自动加载 WebView 页面，不显示顶部线性进度条、百分比或其他持续页面加载进度控件。启动 Splash 和 H5 自身已有业务加载态不在此禁令内，但不得新增与页面加载重复的原生可视进度。

### G19 APK 体积（MUST）

最终交付的签名 Release APK 必须 `>= 10 MiB`（10,485,760 字节）。体积检查在签名前设计、签名后实测，并写入关版结果。不得通过篡改 APK 或在签名后追加文件达标；优先由必要的本地离线资源和兼容依赖自然满足。

### G20 签名连续性（MUST）

正式 keystore 和密码不得进仓库。keystore 由负责人安全保管，关版记录证书 SHA-256；后续版本必须使用同一签名。丢失签名视为发布阻断。

## G. 版本治理

### G21 单版本推进（MUST）

同一时间仅一个大版本为 `IN_PROGRESS`。未关版不得开始下一版。

### G22 关版漂移审计（MUST）

每个大版本完成后必须全局检查：项目目标、硬门禁、页面进度、视觉复用、功能等价、优点复用、踩坑记录、App 兼容和剩余风险。发现漂移时先加固文档/脚本并重测。

### G23 无状态接续（MUST）

状态、下一任务、证据、决策、踩坑和校验和必须更新。任何只能从聊天得知的关键事实都视为未交付。

### G24 范围边界（MUST）

不改 Discuz 管理后台和外部域名内部 UI；但所有外链目标和返回行为必须保留。安全整改、运行时升级和业务新增必须另建任务。

### G25 三环境构建与 CI 失败闭环（MUST）

- 本机负责快速反馈，服务器既有隔离镜像负责正式签名 Release APK，GitHub Actions 负责干净检出和 Android 模拟器独立复验。
- GitHub Actions 必须执行编译、单元测试、Lint、APK 元数据/体积/权限/敏感信息门禁、模拟器安装启动、截图和 crash/ANR 日志检查。
- 任何失败、崩溃、测试红灯或明显 UI 偏差都必须进入“分析→修改→重打包→重测试”，不得用 `continue-on-error`、删除断言、扩大权限或关闭依赖校验制造绿灯。
- CI Debug APK 不能冒充服务器正式签名 Release；keystore、密码、站点凭据、Cookie、Token 和登录后真实个人截图不得进入 Git、Artifact、Cache 或日志。
- 每次关版必须记录服务器产物 SHA-256、GitHub commit/run、Artifact、截图人工结论和剩余实体机验证。
