# 06 全局硬性门禁

任何 AI、开发者、脚本或版本都必须遵守。`MUST` 失败即停止受影响任务，不允许用“之后再补”绕过。

## A. 凭据和生产

### G00 凭据零落盘（MUST）

交接包、源码、提交、日志、截图和 APK 内不得包含账号、密码、数据库密码、私钥、Cookie、Token、验证码、formhash、签名密码或真实身份资料。凭据只通过负责人批准的外部安全渠道提供。

### G01 禁止生产试改（MUST）

没有预发布验证、备份 ID、恢复命令和负责人批准，不得修改 `/www/wwwroot/tg.suewammes.com`。先在预发布副本开发。

生产稳定代码清单必须排除根目录运行时输出 `operation.log`。哈希变化时必须先做逐文件差异，禁止把日志新增误判为代码漂移。

### G02 可恢复（MUST）

每次发布前必须有代码、数据库、上传资源的匹配快照和恢复演练。回滚命令不得依赖未记录的聊天信息。

## B. 业务功能完全一致

### G03 路由/表单协议冻结（MUST）

不得改变：

- URL、插件 `id`、`ac`、`modac` 及其他查询参数名和含义；
- form action、method、字段 name/value、隐藏字段、`formhash`；
- PHP 模板变量、Discuz hook、AJAX 请求/响应形状；
- Intent/支付回调参数和订单关联字段。

允许调整 DOM 时必须保存改前/改后协议 diff，证明业务控件仍存在。

### G04 业务规则冻结（MUST）

不得改变权限、金额、积分、红包、奖励、分红、审核、排序、曝光、提现、支付、注册、认证、注销逻辑；不得改数据库结构、插件启停或后台配置。

### G05 功能等价证据（MUST）

“页面可打开”不等于通过。每个页面必须验证原入口、原动作、成功/失败/空态/权限态及服务端结果，资金类还需账务前后对账。

## C. 页面覆盖和数据副作用

### G06 页面台账（MUST）

所有真实前端点击可达、合法状态可达的弹层、结果页和插件页面都必须进入 `03_PAGE_LEDGER.csv`。每个 `IN_SCOPE` 页面除根入口外必须在 `17_RUNTIME_CLICK_GRAPH.csv` 中有父边。源码存在、直接 URL 返回 200、隐藏 DOM、AJAX 分片或插件启用不能单独成为视觉范围证据。

### G07 GET 副作用隔离（MUST）

详情查看等 GET 可能写浏览、奖励或钱包数据。自动化只允许使用隔离账号和测试内容，并记录前后数据库差异。

### G08 高风险动作授权（MUST）

支付、购买、提现、签到、领取、发红包、发消息、发布、删除、举报、实名、验证码、退出、注销等动作，只能在明确测试用例和隔离数据下执行。生产真实账号不得用于破坏性验证。

## D. H5 视觉与兼容

### G09 视口和安全区（MUST）

360×800、390×844、430×932 均不得横向滚动、窄列竖排、遮挡或不可点。顶部必须同时适配浏览器与 Android App 状态栏，底部适配手势安全区。

### G10 可访问性（MUST）

触控目标至少 44×44px；输入字体至少 16px；正文对比 4.5:1；焦点可见；状态不得只靠颜色；动态内容有可理解反馈。

### G11 本地资源（MUST）

新 UI 运行时不得依赖新增公共 CDN。新设计不得继续调用在线 Tailwind CSS；Tailwind/Font Awesome 等现有 CDN 应在相应版本迁移为本地、可锁定资源。图标优先使用本地 SVG sprite/内联 SVG；需要保留 iconfont 时，只发布实际使用字符的 WOFF2 子集并由本站静态资源服务器长期缓存。不得引入来源不明资产，图标不可因第三方网络失败而消失。

### G12 旧 WebView 兼容（MUST）

不为视觉效果引入未经验证的新框架、重型动画或仅新浏览器支持的关键能力。必须在 Android 系统 WebView 和微信内置浏览器真机验证。

## E. 推广宝品牌

### G13 品牌统一（MUST）

App 名、H5 页面标题、登录/注册、桌面引导、启动页、图标、关于页和可控站内品牌显示统一为“推广宝”。涉及法律主体、合同正文、支付商户名的文字不能擅改，必须由负责人确认。

### G14 新视觉断代（MUST）

新页面必须使用 `STYLE_TOKENS.json` 的“晴空电光”浅色体系，不得继续以暖橙、奶油底、大胶囊作为主体风格，也不得使用大面积深色背景、深色头部或深色启动页。深色仅限文字、描边和小面积图标。业务语义色不受装饰偏好覆盖。

## F. Android App

### G15 原生容器身份（MUST）

- App 名：`推广宝`
- applicationId：`com.suewammes.tuiguangbao`
- 主域名：`tg.suewammes.com`
- 只允许经过配置的 HTTPS 主机在 WebView 内打开。

变更包名属于破坏性产品决定，必须新建书面决策。

### G16 相册与上传（MUST）

优先使用系统 Photo Picker/Storage Access Framework，按 Android 版本请求最小权限。不得申请无必要的全盘存储权限。H5 `input[type=file]` 的 MIME、单/多选、取消和回调必须完整保留。

系统选择器返回的受限 `content://` URI 必须能被 WebView 读取并用于上传；
`allowContentAccess` 不得关闭。同时必须保持 `allowFileAccess=false`，不能为修复
上传而开放 raw `file://`。

### G17 第三方 App 拉起（MUST）

只允许白名单 scheme/intent（包括支付宝实际支付链路）。解析失败时回退到安全 H5 或明确错误；禁止任意 scheme、`javascript:` 外跳和忽略 SSL 错误。必须验证 App→支付宝→App/H5 返回与订单状态刷新。

### G18 状态栏不重叠（MUST）

WebView 必须 edge-to-edge 铺到系统栏后方，原生根容器和 WebView 四边不得添加 WindowInsets padding；H5 单一负责页面自身的 `safe-area-inset-top/bottom`。状态栏可覆盖浅色顶栏背景，但关键控件不得被系统图标遮挡，且状态栏下不得出现第二段空白。浏览器由 Codex 截图验证，App 由负责人真机异步验证。

### G18A 无可视化页面加载进度（MUST）

推广宝 Android 容器自动加载 WebView 页面，不显示顶部线性进度条、百分比或其他持续页面加载进度控件。启动 Splash 和 H5 自身已有业务加载态不在此禁令内，但不得新增与页面加载重复的原生可视进度。

### G19 APK 体积（MUST）

最终交付的签名 Release APK 必须 `>= 10 MiB`（10,485,760 字节）。体积检查在签名前设计、签名后实测，并写入关版结果。不得通过篡改 APK 或在签名后追加文件达标；优先由必要的本地离线资源和兼容依赖自然满足。

### G20 签名连续性（MUST）

正式 keystore 和密码不得进仓库。keystore 由负责人安全保管，关版记录证书 SHA-256；后续版本必须使用同一签名。丢失签名视为发布阻断。

## G. 版本治理

### G21 单版本推进（MUST）

同一时间仅一个大版本为 `IN_PROGRESS`。未关版不得开始下一版。

### G22 关版漂移审计（MUST）

每个大版本完成后必须全局检查：项目目标、硬门禁、页面进度、视觉复用、功能等价、优点复用、踩坑记录、App 兼容和剩余风险。发现漂移时先加固文档/脚本并重测。

### G23 无状态接续（MUST）

状态、下一任务、证据、决策、踩坑和校验和必须更新。任何只能从聊天得知的关键事实都视为未交付。

### G24 范围边界（MUST）

不改 Discuz 管理后台和外部域名内部 UI；但所有外链目标和返回行为必须保留。安全整改、运行时升级和业务新增必须另建任务。

### G25 本机、服务器与异步真机闭环（MUST）

- 自 D-020 起停止 GitHub Actions 自动测试；`.github/workflows/android-ci.yml` 仅保留历史手动入口，不得配置 `push`、`pull_request` 或定时触发，也不得作为后续关版前置条件。
- 负责人电脑禁止由 Codex 安装 Android SDK、ADB、模拟器、系统镜像或其他 APK 测试环境，禁止由 Codex 在本机或服务器安装、启动、操作或功能测试 APK。
- Codex 只负责 Android 源码开发、已有工具下的静态检查，以及在原生源码变化时使用服务器既有隔离镜像完成正式签名 Release APK 构建、体积、包信息、签名和 SHA-256 复验；构建不等于 APK 功能测试。
- 每个可测试大版本的服务器正式签名 APK 必须复制到负责人本机桌面。负责人反馈是异步输入，开发不得停下来等待；收到问题后进入当前版本缺陷队列并执行“分析→修改→重打包→重测试”。
- 上一条中的“重测试”仅指负责人重新进行真机测试；Codex 完成修复、构建和桌面交付后不代替负责人操作 APK。
- 本限制仅针对 APK。H5 页面仍必须由 Codex 在浏览器中实际打开、交互体验和截图比对，检查变形、错位、溢出、遮挡、品牌和浅色视觉。
- 异步不等于免验收：相册真实上传、第三方支付拉起/返回等只能由实体机证明的项目必须保留为累计项目验收门禁。按 D-035，未改 Android/支付/上传协议的纯 H5 视觉部署不等待该反馈；但项目最终负责人验收不得在这些项目没有实体机证据时标记 PASS。
- 任何本机、服务器或负责人反馈中的失败、崩溃、测试红灯或明显 UI 偏差都不得通过删除断言、扩大权限或关闭依赖校验制造通过。
- keystore、密码、站点凭据、Cookie、Token 和登录后真实个人截图不得进入 Git、公开构建产物或日志。
- 每次关版必须记录服务器产物 SHA-256、对应 commit、桌面文件名、构建检查结论、H5 浏览器截图结论和仍待负责人真机验证的项目。

### G26 运行时可达性与隐藏节点（MUST）

- DOM、模板、路由或 200 响应存在不等于用户可达功能；页面台账以改前运行时可见、可点击状态为准。
- 改前零高、`hidden`、`none`、被祖先裁剪、插件开关关闭或条件未满足的节点，改后必须继续不可见、不可点击且不占位。
- 每个可能受 CSS 影响的隐藏节点必须记录计算样式、祖先裁剪后的暴露面积、命中测试、占位高度和截图；只读条件路由必须标为隐藏协议，不得写成用户实点。
- 发现错误显现时必须先撤回、提升本地资源版本、覆盖错误证据并重跑门禁，当前版本不得带漂移关版。

### G27 并行子线程所有权（MUST）

- 并行开发采用一个主线程集成、最多三个子线程执行的模式；子线程必须按互斥页面文件或只读审计/预研切分。
- 主线程独占共享模板/脚本、舞台与生产门禁、状态文档、manifest、版本切换和最终关版；子线程结果未经主线程复核不得合入或作为 PASS。
- 当前版本未 PASS 时，下一版本只允许只读预研，不得编辑代码或标记进度；G21 的单版本推进仍然有效。
- 子线程不得并发修改同一文件。发现所有权冲突或共享依赖时，立即停止该文件编辑并交回主线程统一处理。

### G28 真实点击图定界（MUST）

- UI 改造范围只由当前生产 H5 的真实可见点击链决定；禁止从源码、路由表、直接 URL、猜测参数或开发者工具反推新增视觉页面。
- 入口证据必须同时包含父页面、截图可见、祖先裁剪后暴露面积大于 0、`elementFromPoint` 命中和实际点击后的子 URL。
- 因真实权限、订单或数据条件才出现的入口，必须通过合法状态自然出现；伪造参数得到的页面只能标记为隐藏协议。
- 生产点击会写浏览量、日志、余额或任务时，只在生产记录可见入口和脱敏 href；实际子页面点击在未改 UI 的隔离基线重放并回滚。
- 没有点击图父边的模板不得换色、重排或加入页面截图；公共 CSS 必须证明没有使其显现。
- 发现范围漂移时，当前版本立即保持 `IN_PROGRESS`，相关制品和证据标记失效，先重建点击图并撤销超范围 UI，再继续开发。

### G29 普通 UI 换肤快速通道（MUST）

- 本项目后续普通返修的目标只有：真实可达页面换配色和 UI，业务功能不变。禁止把普通 HTML/CSS 换肤重新扩展为页面考古、隐藏功能开发或重复的大型测试工程。
- 已经取得真实父入口和历史三视口基线的页面，不重复证明入口；只在入口发生变化或负责人指出范围错误时重审点击图。
- 同一批互斥页面一次开发、一次合并、一次预发布、一次回归。默认只在 `390x844` 主视口逐页检查；仅当改动共享布局、断点、安全区或发现溢出时，才补跑 `360x800` 和 `430x932`。
- 默认只保存首页、一个复杂表单/资金页和一个本批代表页截图；缺陷页另存修复前后截图。禁止为了数量重复截取未变化页面。
- 每个实际修改的 H5 页面必须在浏览器中打开体验；同一共享样式影响多个页面时，至少逐页检查几何和溢出，并对代表页截图人工比对，不能只靠静态 CSS 或 HTTP 200。
- 页面模板的路由、表单和业务脚本字节未变时，以静态协议门禁加只读页面冒烟证明功能未漂移；不重复执行支付、提现、奖励、上传、删除等高风险业务动作。
- 每批只保留三项发布门禁：真实入口在范围内、业务协议未改变、备份和回滚可执行。其余检查必须由实际改动风险触发，不能机械全量运行。

### G30 H5 UA 唯一体验口径（MUST）

- 本站业务前端只支持 H5；页面发现、点击图、布局、截图、品牌和范围审计必须使用
  Android mobile UA。App 口径必须在系统 UA 后追加 `TuiGuangBaoAndroid/{versionName}`。
- 桌面 UA 的页面、跳转、DOM、HTTP 结果或截图不得用于证明页面不存在、真实可达、
  可见、可点击、无溢出或视觉验收通过。
- `_blank` 子标签、重定向后的新标签和隔离舞台新标签必须重新核验 UA；UA 未继承时
  先重新施加移动 UA 并重载，再取得子页证据。
- 若先前结论使用桌面 UA，状态必须退回未证明并重测，不得用旧截图补写 PASS。

### G31 定时实质进展与防循环（MUST）

- 活跃开发至少每 30 分钟以及每个实现、验收、部署、清理和关账阶段边界执行一次自查。
- 自查必须列出：上一项新增制品或验证结果、当前唯一下一动作、是否存在重复分析。重复读文件、重复写计划、等待异步反馈或没有新证据的推测不算进展。
- 连续两次自查没有新增制品、测试结果、部署状态或已解决事实时，必须立即停止当前路径并更换方法：执行最小可运行检查、制作最小补丁、切换到互斥子任务，或将确需负责人权限的事项登记为异步门禁。
- 自查检查点写入 `CURRENT_STATUS.yaml.progress_guard` 和 `NEXT_TASK.yaml.self_correction`。禁止以“仍在思考”无限保持同一动作，也禁止为了看似推进而扩大真实点击范围或跳过安全门禁。

### G32 最终项目就绪必须独立非零门禁（MUST）

- R09 H5 开发 PASS 不等于整个项目最终 PASS。`scripts/test-final-project-readiness.ps1` 是最终项目完成、正式标签和最终状态切换的独立前置门禁。
- 负责人真机矩阵必须逐项为 PASS 且引用仓库内脱敏证据文件；安装启动、安全区、相册真实上传、支付宝拉起/返回、可信下载和离线重试不得用服务器构建、浏览器截图或源码检查替代。
- 数据库凭据轮换必须有不含新凭据的证据文件；历史暴露提交不得再被任何本地或远端分支引用。仅从 HEAD 删除字符串不等于历史风险清零。
- 任一项未满足时脚本必须返回非零，`release_status` 保持 `IN_PROGRESS`，不得打最终标签或声明整个项目完成；不受影响的定向返修仍继续。
