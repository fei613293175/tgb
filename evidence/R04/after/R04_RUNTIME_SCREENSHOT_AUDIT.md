# R04 运行时截图审计

- 日期：2026-07-26
- 舞台：`127.0.0.1:18084`，仅 SSH 隧道
- 修正部署：`20260726T233007+0800`
- 请求视口：`360x800`、`390x844`、`430x932`
- 浏览器：Android 14 手机 UA

## 结果

- 首页默认态、搜索空态和真实分类空态各 3 张，共 9 张纠偏后截图。
- 首页原站零高隐藏分类 DOM（`data-id=5/14/15/13`）三档均为暴露高度 0、可见数 0、命中数 0，不占位、不遮挡内容，也不计入用户可达功能。
- 首页搜索输入为 16px / 48px，发布为 56x56，底栏高度 64px；搜索、头条、内容卡片、发布和底栏保持原业务入口。
- 搜索/分类返回和发布操作至少 44px；标题位于固定左右操作区之间，三档无标题或命中框重叠。
- 三类页面横向溢出均为 0；R04 插件页控制台 error/warn 为 0；最终带 request-id 关联的网络重载无失败和 HTTP 4xx。
- 浏览器能力请求视口与截图位图尺寸可能因内置浏览器滚动条/可视区域扣减不同；机器数据同时记录两者，不把文件名当成像素证明。

## 脱敏

- 首页截图仅在当前浏览器 DOM 中临时替换作者、标题、头像和浏览计数；下一次导航恢复，不修改服务器或业务响应。
- 搜索空态截图仅在当前 DOM 将测试查询标题替换为“搜索结果”；不修改 URL、服务器文件或业务响应。
- 截图未保存查询词、Cookie、UID、真实身份或私密计数。
- 首页改造前图复用 R02 的 390x844 登录后截图；R03 未修改首页，因此是 R04 的有效前态。

机器数据：`R04-BROWSER-MATRIX.json`。
