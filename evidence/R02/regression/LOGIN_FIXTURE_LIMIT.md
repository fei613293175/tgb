# R02 登录夹具限制

日期：2026-07-26
状态：`RESOLVED_BY_STAGING_SYNTHETIC_FIXTURE`

## 观察

- 修复 R02 UCenter 客户端配置后，UCenter 查询已不再报 1142 权限错误。
- 给定登录标识不在生产或 R02 的
  `tb_cus_mobilereg_userphone.mobile` 实际登录映射中。
- 扩展只读查找能在其他业务资料字段映射到一个用户，但给定密码与该
  用户的 UCenter 密码不匹配。
- 浏览器提交因此不能形成 R02 已登录会话。
- 未把密码写入仓库、证据或服务器日志；未重置生产或克隆用户密码。

## 判定

这不是 R02 UI 回归，也不能通过修改真实账号或放宽生产认证来绕过。
登录/注册 UI 的公开页面仍可在 R03 改造；登录后页面体验继续使用已存在的
生产只读会话与 R00 页面记录。需要写入的业务回归必须在后续版本创建明确的
隔离测试账号/夹具，而不是复用无法验证归属的克隆账号。

## R02 解决结果

- 仅在 R02 主库和 UCenter 克隆库创建固定命名的合成视觉账号；未修改生产账号。
- 主库和 UCenter UID 一致，Discuz 六个核心关联行完整。
- 密码由服务器随机生成，未输出、未落盘、未进入 Git；浏览器只通过回环临时
  登录桥取得一小时会话。
- 登录桥已于 `20260726T183947+0800` 移出站点并只读归档。
- 强制 Android UA 已于 `20260726T183948+0800` 恢复为动态转发。
- 最终 `POST=405`、登录桥关闭、生产稳定代码不变均再次通过。

因此原负责人账号不可验证的问题不再阻断 R02 登录后视觉取证；负责人账号本身
仍不得被重置或写入仓库。后续受控写入用例仍需按页面族建立独立可回滚夹具。

## 继续规则

- 不等待异步账号反馈，继续完成不依赖登录写入的安全开发；
- 不把登录失败误报为页面功能失败；
- 不保存含手机号、密码、Cookie、formhash 的截图或文本证据；
- R03 可复用现有 staging 合成账号模型，但必须重新开启并回收最小测试窗口；
- R09 生产发布前必须用负责人确认的有效账号完成端到端登录。
