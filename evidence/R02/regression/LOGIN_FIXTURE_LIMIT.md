# R02 登录夹具限制

日期：2026-07-26
状态：`OPEN_NON_BLOCKING_FOR_SAFE_CODE_WORK`

## 观察

- 修复 R02 UCenter 客户端配置后，UCenter 查询已不再报 1142 权限错误。
- 给定登录标识不在生产或 R02 的
  `tb_cus_mobilereg_userphone.mobile` 实际登录映射中。
- 扩展只读查找能在其他业务资料字段映射到一个用户，但给定密码与该
  用户的 UCenter 密码不匹配。
- 浏览器提交因此不能形成 R02 已登录会话。
- 未把密码写入仓库、证据或服务器日志；未重置生产或克隆用户密码。

## 判定

这不是 R02 UI 回归，也不能通过修改真实账号或放宽生产认证来绕过。
登录/注册 UI 的公开页面仍可在 R03 改造；登录后页面体验继续使用已存在的
生产只读会话与 R00 页面记录。需要写入的业务回归必须在后续版本创建明确的
隔离测试账号/夹具，而不是复用无法验证归属的克隆账号。

## 继续规则

- 不等待异步账号反馈，继续完成不依赖登录写入的安全开发；
- 不把登录失败误报为页面功能失败；
- 不保存含手机号、密码、Cookie、formhash 的截图或文本证据；
- R03 进入认证页面改造前重新创建可回滚的隔离认证夹具；
- R09 生产发布前必须用负责人确认的有效账号完成端到端登录。
