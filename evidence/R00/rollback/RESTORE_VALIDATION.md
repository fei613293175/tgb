# R00 恢复验证

执行日期：2026-07-26  
结论：PASS

## 验证方式

- 从同一快照恢复站点代码、主数据库和 UCenter 数据库到生产根之外的隔离舞台。
- 主数据库逻辑转储在同一 MySQL 实例导入到独立数据库，导入后为 `680` 张表。
- UCenter 逻辑转储导入独立数据库，导入后为 `607` 张表。
- 舞台配置已重写为独立数据库用户，不连接生产数据库。
- 舞台 HTTP 最终返回真实登录 HTML `200`，而非静态占位或无关服务。
- 生产代码清单在恢复前后完全一致。

## 恢复材料

- 备份 ID：`20260726T125247+0800`
- 服务器私有恢复说明：`/www/staging/tg-h5-ui-r00/private/RESTORE_README.md`
- 服务器私有事实记录：`/www/staging/tg-h5-ui-r00/private/R00_FACTS.txt`

具体数据库凭据、连接参数和转储文件不复制进交接包。生产灾难恢复必须由有权限的执行者先阅读服务器私有说明，并再次确认目标路径/数据库不是舞台名称后执行。

## 范围说明

本次证明“匹配快照能够恢复为可运行的隔离副本”，没有对生产目录执行覆盖回滚。生产发布前仍需为具体发布文件重新拍摄匹配快照并完成版本级回滚演练。
