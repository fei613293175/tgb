# R03 协议等价与受控认证

## 静态等价

`scripts/test-r03-static.ps1` 对三张认证模板逐项比较表单、输入、按钮、选择器和
文本域的 `action`、`method`、`name`、`id`、`type`、`value`、事件钩子及布尔属性。
结果为：

```text
[R03-STATIC] PASS
overlay_files=14 auth_pages=3 legal_pages=8
protocol=UNCHANGED legal_copy=FROZEN local_ui_assets=PASS
```

密码登录、注册、短信登录的路由、字段、隐藏值和业务端点未改。六份协议正文以及帮助、
关于的业务正文均冻结；只增加本地样式，帮助/关于只改 UI 标题，关于页只替换可控品牌图。
历史 MKT、合同主体、备案、版权和法律含义未改。

## 受控认证测试

- 有效 Run ID：`20260726T203757+0800`
- 密码登录：成功、密码错误、空值均 PASS
- 短信登录：错误验证码、mock 验证码成功均 PASS
- 注册：空提交未创建账号 PASS
- 真实短信接口：未调用
- 数据：只在 R03 克隆库建立临时夹具；测试后已删除
- 窗口回收：Nginx 配置哈希恢复，`POST` 再次为 `405`

验证码由脚本直接写入克隆库并按生产校验路径消费，不保存其值。测试完成后显式检查
唯一夹具、手机号映射、验证码消费和会员总数，再删除所有夹具。

`20260726T203535+0800` 的前一次运行因数据库断言脚本没有可靠传播失败状态而无效；
服务器私有结果目录已写入 `INVALIDATED.env`。任何后续开发者或 AI 都不得引用该 Run
作为 PASS，只能引用上述 `20260726T203757+0800`。

