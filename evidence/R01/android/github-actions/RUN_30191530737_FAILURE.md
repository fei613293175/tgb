# GitHub Actions Run 30191530737

- URL：`https://github.com/fei613293175/tgb/actions/runs/30191530737`
- Job：`89765515575`
- Commit：`581412636c6c05581d05fe5e5240ff0bbc60a7e8`
- 结果：FAIL
- 失败步骤：`Install locked Android SDK packages`
- 精确错误：`Failed to find package 'platforms;android-37'`

## 原因

`android-actions/setup-android` v4 已成功下载固定的 Command-line Tools build
`14742923`（20.0）并运行其 `sdkmanager`，因此本次不再是 PATH 或工具缺失问题。
失败发生在默认稳定通道的包解析阶段：API 37 编译平台尚未出现在稳定通道。

原始日志归档已通过仓库已登录会话下载并脱敏核验；归档本身不进入 Git，
避免提交冗余运行日志或潜在环境信息。

## 第三次修复

1. `setup-android` 继续固定完整 commit SHA 和 Command-line Tools build；
2. 先只安装稳定通道中的 `platform-tools` 与 `build-tools;36.0.0`；
3. 独立执行 `sdkmanager --channel=3 "platforms;android-37"`；
4. 安装后断言 `${ANDROID_SDK_ROOT}/platforms/android-37/android.jar` 存在；
5. 保持 R01 已冻结的 `compileSdk 37 / targetSdk 36`，不通过降级依赖或改变
   目标运行行为规避失败。

修复必须由后续 run 完整通过构建、APK 门禁、Android 36 模拟器、截图和日志检查
才能关闭。
