# GitHub Actions Run 30191460571

- URL：`https://github.com/fei613293175/tgb/actions/runs/30191460571`
- Commit：`aa9bfc3a4cbfb2ed68c63531a031a5fbd7fb5124`
- 结果：FAIL
- 失败步骤：`Install locked Android SDK packages`
- GitHub annotation：进程退出码 `1`

## 原因

第一次修复尝试改为扫描 `$ANDROID_HOME/cmdline-tools/**/bin/sdkmanager`，但当前 `ubuntu-latest` 镜像没有可满足该断言的可执行路径，因此在 `test -x` 停止。Artifact 步骤已按修复只发出 warning，没有遮蔽主因。

## 第二次修复

使用专门负责下载、校验、许可和 PATH 配置的 `android-actions/setup-android` v4，固定到完整 commit SHA `40fd30fb8d7440372e1316f5d1809ec01dcd3699`，并锁定：

- Command-line Tools build `14742923`（20.0）；
- `platform-tools`；
- `platforms;android-37`；
- `build-tools;36.0.0`。

该 Action 使用 Node 24，避免旧 Node 20 强制兼容警告。仍必须由下一次 run 实证成功，不能因配置看起来合理而关闭失败。

