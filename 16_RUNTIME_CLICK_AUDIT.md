# 16 真实前端点击可达性审计

文档状态：`ACTIVE_MUST`  
生效日期：2026-07-27  
触发原因：R05 将直接 URL、隐藏 DOM 和源码路由误计为用户可达页面，负责人以“发布名片”和“评论我的”截图确认范围漂移。

## 1. 唯一范围定义

只有普通真实用户能够从当前生产 H5 的可见控件逐级点击到达的页面和状态，才进入 UI 改造范围。

以下事实单独存在时一律不能进入视觉改造范围：

- PHP、模板、插件或路由文件存在；
- 直接输入 URL 返回 `200`；
- DOM、隐藏弹层、AJAX 分片或事件处理器存在；
- 元素自身有宽高，但被零高或 `overflow:hidden` 祖先裁剪；
- 插件已启用，但当前页面没有可见入口；
- 使用开发者工具、脚本、直接路由或伪造参数才能进入；
- 业务功能、权限、配置或数据条件当前未启用。

这些对象只保留为业务协议或源码映射，分类为 `HIDDEN_PROTOCOL`、`SOURCE_ONLY`、`DORMANT` 或 `DIRECT_URL_ONLY`，不得换色、重排、截图冒充用户实点，也不得因公共 CSS 变得可见。

## 2. 页面进入范围的证据

除根入口外，每个 `IN_SCOPE` 页面必须在 `17_RUNTIME_CLICK_GRAPH.csv` 中有一条完整父边，并同时满足：

1. 父页面来自当前生产 H5 的已证明点击链；
2. 控件在真实截图中可见；
3. 祖先裁剪后的暴露面积大于 0；
4. `elementFromPoint` 命中控件或其子节点；
5. 使用该可见控件实际点击后到达记录的子 URL；
6. 登录、权限、订单等条件来自合法真实状态，不通过伪造参数制造入口；
7. 证据记录父页面、控件文字、脱敏 href、点击前后 URL、认证状态、视口和截图。

可见入口会触发生产数据副作用时，不直接自动点击生产：先在生产记录可见入口和 href，再在未改 UI 的隔离基线重放同一点击；同时记录数据库前后差异和回滚。此例外仍必须证明入口真实可见，不能使用猜测 URL。

## 3. 审计顺序

1. 未登录首页，从首屏开始滚动到底，记录所有真实可见入口。
2. 逐个点击无副作用入口，记录新页面并继续向下遍历。
3. 使用负责人授权账号登录，只在浏览器会话内使用凭据，不落盘。
4. 对个人中心、设置、内容详情和条件页面重复逐级点击。
5. 对详情等 GET 写库页面，生产只取入口证据，隔离基线完成子页面审计。
6. 去重后更新页面台账；没有父边的旧台账行改为 `OUT_OF_VISUAL_SCOPE`。
7. 比较已实施覆盖层，撤销所有超出真实点击图的 UI 变更。

## 4. 当前纠偏事实

- 负责人确认“发布名片”和“评论我的”不是原平台真实前端点击链页面。
- 生产首页旧分类节点虽存在 DOM 和自身矩形，但真实截图不可见，祖先裁剪后不得计入入口；R04 CSS 曾错误强显零高父容器内的子元素，R05 v5 已撤销该漂移。
- 2026-07-27 已实际点击 `首页 -> 我的`。
- 个人中心当前可见入口已发现：消息、设置、双钱包、邀请、团队、认证、刷新卡、头条、超级头条、我的项目、项目审核、客服和 App 下载。
- 个人中心的设置、消息、双钱包、VIP、邀请、团队、认证、刷新卡、头条、超级头条、我的项目、项目审核、App 下载、签到和分红已实际点击；内部 App 下载落地页属于 UI 范围，外部二进制地址不属于。
- 设置页的资料、地址、联系方式、用户协议、隐私政策、消费协议和注销确认已实际点击；退出登录未在只读审计中触发。
- 钱包页的提现设置、收益记录和提现记录已实际点击；真实余额、账户和交易内容未进入证据。
- 消息列表已实际点击；R09 随后用合成会话行完成隔离点击并清理已读相关夹具，会话详情已进入范围。
- 钱包交易列表到成员页的该条边仍未点击；成员页已由 R09 私信详情中的合成消息头像建立另一条完整真实点击父边并进入范围。
- 生产首页项目标题先证明可见父入口，再在 R04 派生的未改 UI 隔离基线实际点击进入详情；浏览量、访问日志和每日任务相关行集均已回滚。
- 隔离详情页的“举报”可见、可命中并已实际点击到举报表单，未提交表单，因此详情页和举报页进入 R05 UI 范围。
- 同一详情页中的评论、点赞、收藏没有可见操作；红包明细、名片新增和名片上传的暴露面积与命中数均为 0，继续排除在视觉范围外。
- 当前个人中心没有发现评论中心、收藏、背景选择或名片入口。
- 旧个人设置、订单和 R09 源码插件仍无真实父边，不得提前进入视觉范围；收银台已由下述 R09 隔离会员购买链独立取得点击证明。
- R05 v3 及其截图只证明直接 URL 可渲染，已标记 `INVALIDATED_SCOPE_DRIFT`，不得作为关版证据。
- R05 v5 真实点击范围包共 12 个文件；真实浏览器矩阵覆盖 360x800、390x844、430x932，详情页无超范围可见操作，390x844 首页旧分类可见数为 0，隐藏控件门禁通过。

## 4.1 R09 公开入口、搜索与法律页收尾

- 所有重放统一使用 Android mobile UA，并追加 `TuiGuangBaoAndroid/1.0.0`；
  桌面 UA 不得用于页面范围或显示结论。
- 登录页可见注册、验证码登录、服务协议和隐私政策入口均完成真实点击。
- 首页搜索的 `_blank` 子页通过用户手势 `Page.windowOpen` 取得真实 URL，子标签
  重新施加 Android H5 UA 后加载 20 条结果且无横向溢出。
- 防骗提醒从隔离详情中的可见 44px 控件点击到达，详情 GET 的 9 次观察增量和
  相关日志/任务行集全部回滚，认证桥、来源桥和 28085 隧道全部关闭。
- 生产 Android H5 个人中心和设置中心均没有会员协议、帮助、关于链接或可见文本；
  三页降为 `OUT_OF_VISUAL_SCOPE / DIRECT_URL_ONLY`，不得以静态文件存在性重新纳入。
- 详见 `evidence/R09/scope-closeout/R09_SCOPE_CLOSEOUT.md`。

## 5. 关版门禁

- `03_PAGE_LEDGER.csv` 的每个 `IN_SCOPE` 行必须能反向追溯到点击图根入口。
- `SOURCE_ROUTE_FOUND`、`SOURCE_DISCOVERED`、`DORMANT_ENABLED` 不能与 `IN_SCOPE` 同时出现。
- 页面截图没有父边和点击结果时，只能作为源码探针证据。
- 每版开始前重放本版点击链；入口消失时立即停止该页面改造并重新定界。
- R05 在点击图重建、超范围覆盖撤销和文档同步完成前不得 PASS。
## 2026-07-27 R09 私信与成员隔离点击补证

- 统一口径：Android mobile UA + `TuiGuangBaoAndroid/1.0.0`，视口 `390x844`。
- 合成账号只存在于 R08 隔离库；生产账号、真实私信正文和真实身份均未进入证据。
- 首页 -> 个人中心 -> 消息 -> 合成会话行 -> 私信详情完成真实点击。会话行裁剪后暴露面积约 `10992.8px2`，中心命中通过。
- 私信详情 -> 合成消息头像 -> 成员详情完成真实点击。头像为 `40x40`，裁剪后暴露面积 `1600px2`，中心命中通过。
- 两个页面因此从候选升级为 `IN_SCOPE`。钱包成员边和详情私信边仍保留各自未点击状态；页面入围不等于伪造每一条父边已经点击。
- 改前截图位于 `evidence/R09/member-chat/before/`；实现与清理状态见 `evidence/R09/member-chat/R09_MEMBER_CHAT_PROGRESS.md`。
- 合成消息、合成 Discuz/UCenter 用户和成员访问记录均已删除；认证桥、来源桥和隧道均关闭，POST 恢复 `405`。
- 最终五文件 v5 换肤已通过静态协议门禁和 `360x800`、`390x844`、`430x932` 浏览器审核，并于 `20260727T142812+0800` 最小部署生产。
- 私信与成员的同一合成点击证据各只保留一条父边；当前点击图不得重复登记同一次点击。未重放的替代入口不得伪造为已点击，也不再重复产生相同页面开发任务。

## 2026-07-27 R09 收银台隔离点击与视觉收尾

- 统一使用 Android mobile UA + `TuiGuangBaoAndroid/1.0.0`，通过隔离会员页中真实可见、可命中的“立即开通”控件实际点击进入 `FINANCE-PAYMENT`；所有交易参数在点击图和证据索引中完全脱敏。
- 收银台因此升级为 `IN_SCOPE / CLICK_PROVEN / REDESIGNED_VERIFIED`。本次点击链没有暴露订单列表父入口，`FINANCE-ORDERS` 收口为 `OUT_OF_VISUAL_SCOPE / DIRECT_URL_ONLY / CLICK_PARENT_UNPROVEN`，不得因同属交易页面而扩大范围。
- 仓库安全制品为 `deliverables/r09-cashier-overlay-v3.tar.gz`，SHA-256 `1d8775866e2abec21ce18824a485d3deaf125106edbbc56ad5b4f19f26264dc7`；它只保存本地 CSS 和哈希前置的精确转换器，不保存含支付敏感值的完整模板。生产最终仍只变更模板与 CSS 两个站点文件；`360x800`、`390x844`、`430x932` 均通过人工截图和几何审核。
- 生产部署 ID 为 `20260727T152930+0800`。浏览器审核没有点击支付动作；隔离交易夹具已清理，结果为 `PASS`，文档不保存订单号、UID、余额或账号。
- 去除两条重复的私信/成员边并新增唯一会员到收银台父边后，当前点击图为 48 条边：41 条实际点击、2 条同页替代入口、5 条隐藏或不存在入口；点击证明页面为 38 类，待隔离点击为 0。
