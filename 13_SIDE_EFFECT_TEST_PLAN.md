# 页面副作用测试计划

文档状态：R00 冻结候选  
适用范围：`03_PAGE_LEDGER.csv` 与 `12_PAGE_SOURCE_MAP.csv` 中全部页面  
核心原则：视觉改造不得改变业务功能、数据语义、金额、权限、跳转目标或第三方回调。

## 1. 测试环境分层

### 1.1 当前 R00 只读镜像

- 入口仅监听 `127.0.0.1:18081`，只能通过 SSH 隧道访问。
- Nginx 只允许 `GET`、`HEAD`，其他方法返回 `405`。
- 用途：模板覆盖盘点、页面截图、资源错误、布局与导航检查。
- 禁止：登录提交、短信、发布、评论、收藏、上传、聊天、提现、支付、购买、签到、注销等写操作。

### 1.2 受控写入窗口

仅在对应版本的任务文档明确开启后使用，并按以下顺序执行：

1. 重新拍摄数据库和附件目录快照，记录快照 ID 与哈希。
2. 创建专用测试用户、测试内容、测试订单等合成夹具，不使用真实用户数据做破坏性验证。
3. 将短信、支付、提现、外部分享等出口替换为舞台环境 mock 或明确批准的沙箱。
4. Nginx 只对白名单路由临时放行所需方法，不开放全站写入。
5. 每个用例执行前后记录数据库行、关键字段、余额和文件清单差异。
6. 用例结束后回滚夹具与附件，关闭写入窗口，重新验证 `POST=405`。

## 2. 风险等级与最低证据

| 等级 | 典型页面 | 最低验证 |
|---|---|---|
| LOW | 静态协议、启动页、离线页 | HTTP 状态、截图、链接、资源 4xx、文案冻结 |
| MEDIUM | 首页、搜索、分类、会员页、设置、聊天列表 | LOW + Cookie/缓存/访问日志说明 + 关键表不变 |
| HIGH | 详情、评论、发布、资料、地址、钱包、订单、上传 | 前后数据库断言 + 附件差异 + 回滚结果 |
| CRITICAL | 认证、注销、聊天发送、提现、支付、购买、签到、奖励领取 | HIGH + 出口隔离 + 金额守恒 + 幂等/重复提交 + 回调/返回链 |

`GET` 不默认等于无副作用。内容详情、红包列表、会员访问等页面可能写访问记录、浏览量或奖励日志，必须用前后断言证明实际行为。

## 3. 页面族测试矩阵

### 3.1 登录与注册

- 只读：登录、注册、短信登录、验证码错误/过期状态的布局与资源。
- 写入窗口：仅测试账号；短信出口 mock；断言用户表、登录日志、验证码表只出现预期变化。
- 不允许把账号、密码、短信密钥、第三方配置写入证据或开发文档。

### 3.2 内容发现与详情

- 首页、搜索、分类、文章列表：只验证原站运行时可见筛选、分页、空态与返回路径；隐藏或零高 DOM 只做条件协议检查，不得模拟成用户点击功能。
- 详情、会员、红包列表：访问前后比较内容浏览计数、访问日志、奖励日志与账户余额。
- 如果旧系统的 GET 本来产生副作用，新视觉层必须保持同一行为；若旧系统没有副作用，新视觉层不得新增。

### 3.3 内容写入与附件

- 评论、收藏、发布、我的发布、封面、举报、名片、资料、地址、联系资料。
- 每个用例只作用于合成夹具；记录主表、关联表、附件目录和缩略图目录差异。
- 验证提交中、成功、失败、重复点击、返回重进、网络中断和超时。
- 文件上传同时验证 Web H5 与 Android 文件选择器，不扩大到全盘存储权限。

### 3.4 聊天

- 列表只读：会话排序、未读数、空态、头像失败占位。
- 详情写入：仅合成对端；断言文本、图片、红包状态、未读计数和时间排序。
- 外部通知与真实红包资金出口必须隔离；重复提交不能产生重复扣款或重复消息。

### 3.5 钱包、订单、提现、支付与购买

- 钱包、订单的只读访问必须保持余额和订单状态不变。
- 提现、VIP、刷新包、头条、超级头条、支付只允许沙箱或 mock。
- 每个用例必须断言：订单唯一性、金额与币种、余额守恒、状态机、失败回滚、重复回调幂等、取消、App 返回前台后的状态恢复。
- Android 外部支付只允许明示 scheme/host 白名单；无法拉起时使用受控浏览器回退，不执行任意 `intent://`。

### 3.6 签到、分红、认证与注销

- 签到/分红：只对夹具账本执行，验证当天重复操作幂等和奖励守恒。
- 实名认证：只使用合成身份与 mock 支付/审核，不收集或保存真实身份材料。
- 注销：只允许专门的一次性夹具账户；执行前必须可恢复快照，执行后验证会话失效与关联数据处理符合旧业务。

## 4. 通用数据库/文件断言

每个 HIGH/CRITICAL 用例至少记录：

```text
case_id
snapshot_id
fixture_ids
request_method_and_route
before_row_counts
before_key_fields
before_balance_fields
before_attachment_manifest
after_row_counts
after_key_fields
after_balance_fields
after_attachment_manifest
expected_delta
actual_delta
rollback_result
outbound_mock_log
evidence_paths
```

数据库表名以部署时实际表前缀解析，不在测试脚本中假定固定前缀。证据中隐藏密码、Cookie、Token、手机号、身份信息、支付参数和回调密钥。

## 5. 视觉层不得触碰的业务边界

- 不重命名后端表单字段、AJAX 参数、路由参数和回调参数。
- 不改变按钮的业务含义、权限条件、金额计算、订单状态、奖励条件和跳转目标。
- 允许增加纯展示容器、CSS class、设计 token 和不改变提交语义的交互反馈。
- 若必须修改 JavaScript，只做视觉状态适配，并以旧事件处理器为行为基线逐项回归。
- 对内联 HTML 控制器（`xigua_member/profile.inc.php`、`view/module/site/sign.php`）单独审查，不能依赖全局模板覆盖。

## 6. 版本执行顺序

- R00：只读镜像、源码映射、副作用计划与回滚演练。
- R02-R06：按页面族逐批开启最小写入窗口，完成 UI 与业务等价验证。
- R07：统一执行财务/支付/购买 CRITICAL 沙箱验证。
- R08-R09：增长、奖励和残余插件的夹具验证与运行时可达性关账。
- R09：在残余页面关账后关闭全部写入窗口，执行全量回归、生产发布演练与最终证据审计。

任何 HIGH/CRITICAL 页面若没有前后断言和回滚证据，不得标记版本完成。
